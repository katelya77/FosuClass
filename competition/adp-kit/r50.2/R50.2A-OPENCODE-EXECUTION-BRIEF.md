# R50.2A OpenCode Execution Brief

Date: 2026-08-19
Branch: `feat/campusflow-adp-integration`
PR: #49 — MUST remain OPEN / UNMERGED

## Mission

Execute the approved R50.2A implementation plan end-to-end with TDD. Do not start R50.2B Widget implementation in this run.

Read first, in this order:

1. `docs/superpowers/specs/2026-08-19-r50.2-unified-native-widget-system-design.md`
2. `docs/superpowers/plans/2026-08-19-r50.2a-interaction-semantics-prompt-hardening.md`
3. `competition/adp-kit/r50.1/2026-08-18-r50.1.2-final-report.md`
4. `competition/adp-kit/r50.1/R50.1-E2E-MATRIX.md`
5. `competition/adp-kit/r50/agents/shared/*.md`
6. `competition/adp-kit/r50/agents/main-orchestrator.md`
7. `competition/adp-kit/r50/agents/schedule-space.md`
8. `competition/adp-kit/r50/agents/risk-planning.md`
9. `competition/adp-kit/r50/agents/campus-insight.md`
10. `competition/adp-kit/r49-ma/tools/adapter/adapter.js`
11. relevant tests under `competition/adp-kit/r49-ma/tests/`.

Use Superpowers TDD/executing-plan discipline. Implement tasks in plan order; do not skip RED verification.

## Frozen constraints

- Main + Schedule + Risk + Insight topology stays unchanged.
- Main does not bind or directly call CampusTools.
- Main -> children, children -> Main only; no child -> child semantic handoff.
- New user turns are Main-first.
- 13 unique Agent Tool operations remain 13; do not add a 14th operation.
- Tool Direct Result remains OFF.
- Dataset remains `competition-demo-v3`.
- No production data, real school identity, real timetable, token, NodeID, VarBizID, or system-prompt secret in repo assets.
- Do not publish the ADP application.
- Do not merge PR #49.

## Real console evidence that this run must address

### 1. Browse/list over-clarification

A broad request to list some teachers was needlessly clarified before `campus_entity_search` list mode was used.

Fix generically with a `BROWSE_LIST` goal family. If a known entity category can return a safe bounded list without a required filter, execute rather than asking for optional precision.

### 2. Availability vs planning intent

A request to arrange a meeting and provide candidate slots routed to `campus_common_free_time_query`, although the actual user goal was planning and `campus_group_plan` exists.

Required generic distinction:

- `AVAILABILITY_DISCOVERY` -> `campus_common_free_time_query`
- `GROUP_PLANNING` -> `campus_group_plan`

Do not require the user to literally mention a room before recognizing group-planning intent.

### 3. Reschedule optional room serialization

Direct tool validation succeeds when `target.room` is omitted, but global Multi-Agent debug produced an entity lookup for an empty room representation.

At the deterministic adapter boundary, normalize optional semantic empties (`null`, blank/whitespace string, `[]`, `{}`) as absent according to schema optionality. Required values and valid numeric values must not be erased.

A reschedule request without target room must not trigger a fake room lookup and must not force a room clarification if the tool can evaluate the required target slot without one.

## Prompt hardening goals

The current R50.1 compiled prompts are functionally rich but too verbose and contain instruction-layer details that belong in deterministic code/tests. Refactor the canonical source under `competition/adp-kit/r50/agents/`, not the compiled artifacts.

Resolve at least these prompt issues:

1. Replace “Agent never outputs final numbers” with the correct rule: the Agent may summarize numbers returned by the current verified tool result, but must never derive/invent new dynamic campus facts.
2. Clarification applies only to missing required semantics, not optional fields.
3. Keep R50.2A text-first. Do not imply Agent Output Widget is already active.
4. Remove obsolete release-label explanations and implementation trivia where they dilute model instructions.
5. Keep essential invariants: tool-only dynamic facts, explicit > inherited, stale escape, no silent week=1, stable-position TopN, source-aware ranking, no mutation claims, internal protocol isolation.
6. Preserve the exact four-Agent tool ownership boundaries.
7. Optimize for clear decision order: role -> owned goals -> tool choice -> clarification rule -> context rule -> result/failure rule.

## ADP AI one-click optimization rule

Do NOT use platform-generated wording as canonical source in this implementation run because OpenCode does not own the live console state.

Instead, create the repo guide required by the plan. The live-console procedure is:

1. repo R50.2A compiled prompt is baseline;
2. user saves/copies baseline in ADP;
3. user clicks AI 一键优化 for one Agent;
4. optimized text is treated as a candidate;
5. candidate is diffed against canonical semantic invariants;
6. only useful structural/wording improvements are selectively merged into source;
7. compiler/tests rerun;
8. one Agent at a time.

Also apply the same candidate-only principle to ADP AI-generated transfer descriptions.

## Required implementation sequence

Follow the detailed plan exactly:

1. Add R50.2A semantic acceptance gate and product E2E matrix.
2. Reproduce optional-room failure with RED tests.
3. Implement schema-aware optional normalization; GREEN focused tests.
4. Add generic goal intent families and route rules; GREEN semantic tests.
5. Simplify/harden all shared + four domain prompts without weakening contracts.
6. Recompile prompts and create R50.2 console snapshots.
7. Add controlled ADP AI optimization guide.
8. Redesign old D1~D6 into Protocol Smoke + natural Product E2E.
9. Run full focused + package regression/security checks.
10. Write `competition/adp-kit/r50.2/2026-08-19-r50.2a-final-report.md`.
11. Commit in small reviewable commits and push the existing feature branch.

## Required verification

At minimum run:

```bash
cd competition/adp-kit/r49-ma
node --test tests/test-r50-2-optional-normalization.js \
  tests/test-r50-2-interaction-semantics.js \
  tests/test-r50-prompt-architecture.js \
  tests/test-r50-1-prompt-convergence.js \
  tests/test-r50-1-e2e-matrix.js \
  tests/test-r50-new-tools.js \
  tests/test-context-model.js \
  tests/test-adp-http-boundary.js \
  tests/test-adp-smoke.js

cd ../r50
node build-agent-prompts.js --check

cd ..
npm test

git diff --check
git status --short
```

Run existing privacy/security gates documented by the current branch as well.

## Final response back to the user

Return a compact execution report containing:

- final branch HEAD;
- commit list created in this run;
- exact prompt before/after sizes for Main/Schedule/Risk/Insight;
- optional-room RED reproduction and GREEN result;
- focused test pass/fail counts;
- full package test result;
- operation count and tool-binding count after implementation;
- files created/modified;
- whether any CloudBase deployment occurred (default should be NO in this run);
- PR #49 state confirming OPEN / UNMERGED;
- explicit list of the four new R50.2A prompt snapshot paths;
- next manual ADP console steps for baseline paste and AI one-click candidate evaluation.

Do not claim Console E2E passed unless it was actually executed in the Tencent ADP console by the user or with real console access.