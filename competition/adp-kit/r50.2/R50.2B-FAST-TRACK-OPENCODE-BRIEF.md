# R50.2B Fast-Track OpenCode Execution Brief

Date: 2026-08-19
Branch: `feat/campusflow-adp-integration`
PR: #49 — MUST remain OPEN / UNMERGED
Baseline R50.2A HEAD: `c043c688469cfc77a138693d885a42f896353092`

## Mission

Execute the approved R50.2B Native Unified Widget stage end-to-end in one repo-side run. Optimize for throughput: do not stop after each subtask for user approval. Finish implementation, tests, console bundle, acceptance matrix, report, commits and push before returning.

Read first:

1. `docs/superpowers/specs/2026-08-19-r50.2-unified-native-widget-system-design.md`
2. `docs/superpowers/plans/2026-08-19-r50.2b-unified-native-widget-fast-track.md`
3. `competition/adp-kit/r50.2/2026-08-19-r50.2a-final-report.md`
4. `competition/adp-kit/r50.2/R50.2A-PRODUCT-E2E-MATRIX.md`
5. `competition/adp-kit/r50.2/prompts/*.final.md`
6. existing native Widget assets under `competition/adp-kit/widget/native/`, especially action contracts, compilers and representative Widget folders.
7. relevant R50/R50.1/R50.2 tests.

Use TDD. Record RED once per new contract family, then move quickly to GREEN. Do not ask the user to manually configure ADP during this repo implementation run.

## Frozen constraints

- Main + Schedule + Risk + Insight unchanged.
- 13 unique Agent Tools / 14 bindings unchanged.
- Main binds no CampusTools.
- Main-first every new user turn.
- Main -> child and child -> Main only.
- Tool Direct Result OFF.
- Dataset `competition-demo-v3`.
- Main remains text + clarification Widget.
- Schedule/Risk/Insight are the only Agent Output Widget targets.
- No formal app publish.
- No PR merge.
- No real school data, real timetable, token, NodeID, VarBizID, internal URL or secrets in assets.

## Chosen Widget architecture

Build ONE reusable native Widget:

`campus-result-unified-v1`

Do not create 13 tool-specific Widgets.

The Widget renders canonical `CampusResultEnvelope` variants:

- schedule
- space
- collaboration
- risk
- reschedule
- ranking
- overview
- empty
- error
- message

Primary flow:

CampusTools verified result -> deterministic projection -> CampusResultEnvelope -> Agent Output Widget.

The projection may format/select facts but may not invent new campus facts.

## Tencent platform constraints to honor

- Agent Output Widget is for final structured result presentation and disables streaming; keep result cards compact.
- Use official Widget Actions only.
- Use `sys.chat` for conversational continuation.
- `sys.chat` payload becomes new conversation input; make payload user-semantic and Main-routable.
- Do not use `alwaysEnabled=true` for normal `sys.chat` buttons.
- Tool Direct Result remains OFF because cross-domain reasoning and summarization are still required.
- Main clarification remains separate from result Widget output.

## Efficiency requirements

### A. Batch prompt handling

Do not make the user review four AI-optimized prompts separately.

Create a batch gate:

`competition/adp-kit/r50.2/prompt-candidate-gate.js`

Expected workflow after repo completion:

1. user pastes current four R50.2 prompts into ADP;
2. user optionally clicks ADP “AI 一键优化” for all four Agents in one console session;
3. candidate text is saved into `r50.2/prompt-candidates/`;
4. one gate command checks all four candidates together;
5. only flagged semantic drift requires manual review.

Gate at minimum detects:

- dynamic fact source weakening;
- tool ownership drift;
- Main direct CampusTools use;
- child -> child semantic transfer;
- silent week=1/default-time behavior;
- optional-field over-clarification;
- TopN stable-position/ranking source drift;
- reschedule mutation claim;
- internal protocol leakage.

### B. One-session ADP cutover

Generate `R50.2B-ADP-BATCH-CUTOVER.md` with the shortest safe sequence:

1. import/select `campus-result-unified-v1` once;
2. update four prompts/settings in one pass;
3. Main output=text + clarification Widget;
4. Schedule/Risk/Insight output=Widget -> unified Widget;
5. verify Tool Direct Result OFF;
6. verify transfer matrix once;
7. do not re-import the 13-op plugin unless signatures actually changed;
8. do not publish;
9. run one 12-family acceptance session.

Include a fast rollback: set three children back to text output + restore R50.2A prompt snapshots. No plugin/tool rollback should be necessary.

### C. One acceptance matrix, not separate D1~D6 loops

Create one matrix covering:

1. broad browse/list without needless clarification;
2. relative academic-week resolution;
3. common availability discovery;
4. group planning uses `campus_group_plan`;
5. ranking tie with stable Top positions;
6. schedule card + follow-up;
7. reschedule with omitted room, no fake lookup, no mutation claim;
8. classroom/space result;
9. overview result;
10. verified empty;
11. recoverable error;
12. `sys.chat` action across domains returns through Main-first routing.

Each case needs only expected Agent/tool family, Widget variant/text fallback, forbidden behavior and screenshot slot. Avoid brittle fixture-value assertions in natural Product E2E.

## Implementation expectations

Follow the detailed plan exactly. Important implementation decisions:

- Keep the `CampusResultEnvelope` contract deterministic and presentation-only.
- Support ranking ties honestly.
- Show weekday/weekend clearly in collaboration candidates; do not silently filter weekend.
- Reschedule card must clearly state simulation/未执行.
- verified empty/error are first-class variants.
- no raw evidence/internal context shown.
- Widget rendering failure must preserve a text fallback derived from verified results.
- reuse the existing native Widget compiler/toolchain and component conventions.
- reuse visual/action ideas from existing seven Widget assets where useful, but do not preserve old Workflow architecture.

## Required verification

At minimum run all new R50.2B tests plus:

- R50.2A semantic + optional-normalization + prompt gates;
- R50 prompt compiler `--check`;
- existing native Widget compiler/audit/contract tests;
- `competition/adp-kit` full test chain;
- root agent foundation;
- root agent regression;
- AI competition;
- final convergence;
- privacy/security gates;
- `git diff --check`.

If `sync-assets-manifest --check` fails:

- if R50.2B legitimately adds tracked generated Widget assets and the manifest is expected to change, update/regenerate it correctly;
- otherwise classify it with evidence and do not hide it.

## Deployment rule

Do NOT deploy CloudBase merely for Widget work.

Deploy only if this implementation genuinely changes the runtime/API required by the live ADP plugin. If no runtime contract changed, CloudBase deploy must remain NO.

## Final response required

Return only a concise execution report containing:

- final HEAD SHA;
- commits;
- files added/modified;
- R50.2B test counts;
- full gate status;
- unified Widget path and import asset paths;
- envelope variants supported;
- prompt candidate gate command/result;
- exact console bundle path;
- exact 12-case acceptance matrix path;
- whether CloudBase was deployed and why;
- PR #49 OPEN/UNMERGED verification;
- any remaining manual ADP step.

Do not claim Console GOLDEN because the final live-console batch acceptance is performed by the user.
