# R50.1.2 Control-Plane SSOT Closure — Final Report

日期：2026-08-18（+08:00）
分支：`feat/campusflow-adp-integration`（PR #49 保持 OPEN / UNMERGED）
交付物：Tool Binding 控制面单一真源（SSOT）+ Visibility 生成器收敛；本轮不部署 CloudBase、不触碰 R50.2 Widget/UI。

## 1. HEAD 语义

- `initialHead = 8170049245431fcc12f5c5abaad818beca61c501`（fetch 后与基线一致；`git pull --ff-only` 无新提交）
- `finalHead`：见报告提交后 `git rev-parse HEAD`（本段不预写自引用 SHA）

## 2. 根因

R50.1.1 已修正 CUTOVER / ACCEPTANCE / 契约测试；但 **`generate-tool-visibility.js` 的手工 `BINDINGS` 对象**与**生成的 `R50.1-TOOL-MODEL-VISIBILITY.md`** 仍携带 R50.1 pre-fix 的旧 Agent binding map：

| Agent Tool | 旧（错误） | 正确 |
|---|---|---|
| campus_entity_search | 课程空间 7 / 校园洞察 3 | 仅课程空间（Schedule） |
| campus_academic_context | Main 0（辅助决策） | 课程空间（Schedule）＋ 风险规划（Risk） |
| campus_common_free_time_query | 课程空间 7 / 校园洞察 3 | 仅课程空间（Schedule） |
| campus_reschedule_feasibility | 课程空间 7 | 仅风险规划（Risk） |
| campus_room_utilization_query | 校园洞察 3 | 校园洞察（Insight）✓（本来就对） |
| campus_group_plan | 课程空间 7 | 课程空间（Schedule）✓（本来就对） |

纯 Control Plane 文档/生成器漂移；Runtime / Prompt / CampusTools 算法无问题。

## 3. SSOT 文件（唯一真源）

`competition/adp-kit/r50.1/agent-tool-bindings.json`：

- `version = "R50.1.2"`；`uniqueOperationCount = 13`；`bindingCount = 14`
- `agents.main = []`（Main 不绑定 CampusTools）
- `agents.schedule =` campus_schedule_query / campus_schedule_range_query / campus_classroom_search / campus_entity_search / campus_academic_context / campus_common_free_time_query / campus_group_plan（**7**）
- `agents.risk =` campus_risk_check / campus_day_plan / campus_academic_context / campus_reschedule_feasibility（**4**）
- `agents.insight =` campus_overview / campus_teacher_load_query / campus_room_utilization_query（**3**）
- `sharedTools = { "campus_academic_context": ["schedule", "risk"] }`（唯一跨域共享）

## 4. Visibility Generator 修改

`r50.1/generate-tool-visibility.js`：

1. 删除手工 `BINDINGS` truth source；绑定列由 `agent-tool-bindings.json` 派生（`bindingsOf` 遍历 agents 求所属集合，共享工具输出「课程空间（Schedule）＋风险规划（Risk）」）
2. 参数 required/optional/console-only 仍以 `campus-agent-tools.r50-existing-plugin-additions.json` 为真源；`baseDate` 仍 console-only
3. Direct Result 仍全部 OFF；模型可见性表（DeepSeek V4 Flash / 高级设置行 / Main 可用工具 / Direct Result / 不可见项）不变
4. 新增 `--check` 幂等模式：输出与磁盘不一致时 `[fail]` + exit 1（契约 K 直接调用）

## 5. Visibility 文档结果（已重新生成）

总览表 6 行：

- entity_search → 课程空间（Schedule）
- academic_context → 课程空间（Schedule）＋风险规划（Risk）
- common_free_time → 课程空间（Schedule）
- room_utilization → 校园洞察（Insight）
- reschedule_feasibility → 风险规划（Risk）
- group_plan → 课程空间（Schedule）

新增绑定规则陈述：13 unique / 14 绑定、academic_context 唯一跨域共享、Main 不直接绑定。旧错误组合（entity_search→Insight、common_free_time→Insight、reschedule_feasibility→Schedule、academic_context→Main）已全部消失。

## 6. RED → GREEN 证据

probe（`%TEMP%\opencode\probe-r5012-red-green.js`，只读 `git show 8170049`，不污染工作树；复用新契约测试的解析逻辑）：

- **PRE-FIX（8170049 旧文档 + 旧生成器）**：**FAIL**
  - campus_entity_search: got [schedule,insight] expected [schedule]
  - campus_academic_context: got [] expected [schedule,risk]（旧值 Main，无域可解析）
  - campus_common_free_time_query: got [schedule,insight] expected [schedule]
  - campus_reschedule_feasibility: got [schedule] expected [risk]
  - generator 仍含手工 BINDINGS；未从 agent-tool-bindings.json 派生
- **POST-FIX（工作树）**：**PASS**（6 工具绑定全部正确，含本就对的两个正确项）

## 7. 契约测试（`test-r50-1-2-control-plane-ssot.js`，12/12 PASS）

| 契约 | 内容 | 断言方式 |
|---|---|---|
| A | SSOT 13 unique operations / 14 bindings | 计算 unique 集与绑定和，对比 JSON 声明 |
| B | Main = 0 CampusTools | deepEqual([]) |
| C | Schedule exact 7 | 顺序+集合 deepEqual |
| D | Risk exact 4 | 顺序+集合 deepEqual |
| E | Insight exact 3 | 顺序+集合 deepEqual |
| F | campus_academic_context 唯一合法重复 | 跨 agent 计数 + sharedTools 声明 |
| G | 其它工具不得跨 Agent 重复 | 重复出现集合=={academic_context} |
| H | CUTOVER 与 SSOT 一致 | 解析 CUTOVER 每 Agent 行工具序列（真实 tool set） |
| I | ACCEPTANCE T1 与 SSOT 一致 | 解析 `- Agent：…。` 严格映射行 |
| J | TOOL-MODEL-VISIBILITY 与 SSOT 一致 | 解析绑定列→agent 集合；13/14 + 唯一共享 + Main 陈述 |
| K | 生成器 `--check` 幂等 | execFileSync 退出码 + `[pass]` |
| L | OpenAPI 全量 13 ops 与 SSOT unique 一致 | adp-import.json 13 operationId 集合对比 |

## 8. 验证结果

| 验证项 | 结果 |
|---|---|
| R50.1.2 契约测试 | 12/12 PASS |
| R50.1.1 契约测试（回归） | 3/3 PASS |
| r49-ma 全套 `node --test "tests/*.js"` | **274/274 PASS**（262 基线 + 12 新增） |
| mcp `npm test` | **52/52 PASS** |
| mcp `npm run check` / `typecheck` / `check:openapi` | PASS / PASS / PASS（14 工具定义一致） |
| `build-agent-prompts.js --check` | PASS（4 files） |
| `sync-campusflow-function.js --check` | PASS（11 files 一致） |
| `test-http-function.js` | PASS（health 200 / 13 Agent Tool Façade / V3 runtime） |
| Golden `eval-golden.js` | **33/33 PASS**，v1、sha1:fefef4bf425b、verified=100% |
| `git diff --check` | 干净（修复生成器 EOF 空行后） |

## 9. 范围纪律核对

- 未修改：CampusTools 业务算法、CloudBase Function Runtime、13 façade HTTP 行为、competition-demo-v3、Endpoint、Token/Authorization、4 个 R50.1 final Prompt（`git diff 8170049..工作树 -- r50.1/prompts` = 空）、Widget、Agent avatar、Chat background、Model A/B
- 未部署 CloudBase
- 本轮改动仅 4 文件：新增 `agent-tool-bindings.json`、`test-r50-1-2-control-plane-ssot.js`；重写 `generate-tool-visibility.js`；重新生成 `R50.1-TOOL-MODEL-VISIBILITY.md`（+ checkpoint）

## 10. CI

PR #49 @ initialHead 8170049：**5/5 SUCCESS**（Xiaofu Agent CI agent-release-gate / Competition ADP Widget CI widget-contract / Public Security Gate ×2 / Admin CI admin-checks）。push 后对新 finalHead 重跑。

## 11. Console 下一步（用户动作，未变）

按 `r50.1/R50.1-CONSOLE-ACCEPTANCE.md` 执行 D1~D6 真机验收（Agent Cutover 按规范绑定表 + 4 个 final Prompt 粘贴），完成并填证据表后宣佈 Console GOLDEN；R50.2（Widget 等）未启动。

## 12. 判定

**`R50.1.2 CONTROL-PLANE GOLDEN`**（repo + CI 全绿）；Console GOLDEN 待 D1~D6。
