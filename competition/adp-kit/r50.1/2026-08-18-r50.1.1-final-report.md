# R50.1.1 Console Contract Consistency — Final Report

日期：2026-08-18（+08:00）
分支：`feat/campusflow-adp-integration`（PR #49 保持 OPEN / UNMERGED）
交付物：R50.1.1 远程修复（ChatGPT 19 commits）的本地最终验证与收口；本轮不部署 CloudBase、不触碰 R50.2 Widget/UI。

## 1. HEAD 语义（防自引用回填）

- `implementationBase = 19da961`（R50.1 收尾 HEAD，远程 19 commits 的基线）
- `initialHead = d462031b94e7bdef48cc76de00538d4064ea55d6`（git fetch → fast-forward 19da961..d462031 后的 HEAD，与 ChatGPT 确认 HEAD 一致）
- `verificationHead = d462031`（全量验证均在 initialHead 执行；RED 证据基于 `git show 19da961` 只读快照）
- `reportCommit = intentionally omitted / self-referential`（本报告提交前不回填自身 SHA；R50.1 报告 `reportCommit=38dfee5` 不重复回填）

## 2. 根因与修复范围（远程 19 commits 已落地）

- **根因 A（控制面文档漂移）**：`r50.1/R50.1-ADP-CONSOLE-CUTOVER.md` 的 §3 手工绑定表错误（Schedule 缺 `campus_academic_context`、错含 `campus_reschedule_feasibility`；Risk 错含 `campus_schedule_query/campus_entity_search`、缺 `campus_academic_context/campus_reschedule_feasibility`），并含错误声明「每个 Agent Tool 恰好出现一次」。Prompt 架构与测试从未漂移。
- **根因 B（类型契约滞后）**：`mcp/campus-tools-mcp/src/contracts.ts` 仅声明 `competition-demo-v1 | v2`，与 runtime v3 数据不闭合；`DATA_VERSION` 默认值保持 `competition-demo-v1` 不变（runtime 版本由 `loadDataset()` meta.dataVersion 决定）。
- 修复落点（19 commits，`dfb931c`…`d462031`）：spec/plan、锁绑定契约测试（`test-r50-1-1-console-contract-consistency.js`）、锁 v3 类型契约测试（`test/data-version-contract.test.js`）、CUTOVER §3 规范化、ACCEPTANCE T1 修正、v3 进入类型联合（`9f7738f`）、MCP 测试门纳入新测试（`d0a7a26`）、package metadata 保持不动（`d462031`）。

## 3. 规范绑定表（truth source = 测试 + CUTOVER §3 + ACCEPTANCE T1，一致）

| Agent | CampusTools 绑定 |
|---|---|
| Main 主控 | 0（仅 KnowledgeRetrievalAnswer + Agent 转移，不直接绑定） |
| 课程空间（Schedule） | 7：schedule_query / schedule_range_query / classroom_search / entity_search / academic_context / common_free_time_query / group_plan |
| 风险规划（Risk） | 4：risk_check / day_plan / academic_context / reschedule_feasibility |
| 校园洞察（Insight） | 3：overview / teacher_load_query / room_utilization_query |

- 13 个 unique Agent Tool = 14 个绑定；`campus_academic_context` 是唯一合法跨域共享（Schedule + Risk）；无其它跨域重复；Main 无直接绑定。

## 4. RED → GREEN 证据（不污染工作树）

- **PRE-FIX（`git show 19da961` 快照，只读）**：FAIL
  - Schedule 行漂移：got `[schedule_query, schedule_range_query, classroom_search, common_free_time_query, reschedule_feasibility, group_plan, entity_search]` expected 规范 7 元组（缺 academic_context、错含 reschedule_feasibility）
  - CUTOVER 缺「14 个绑定」陈述；含错误声明「每个 Agent Tool 恰好出现一次」
- **POST-FIX（HEAD 工作树）**：PASS（同一 probe：复用正式测试的解析/断言逻辑）
- probe 临时脚本：`%TEMP%\opencode\probe-r5011-red-green.js`（仅验证用，不进入仓库）

## 5. 验证结果（verificationHead = d462031）

| 验证项 | 结果 |
|---|---|
| R50.1.1 绑定契约测试（test-r50-1-1-console-contract-consistency.js） | **3/3 PASS** |
| r49-ma 全套 `node --test "tests/*.js"` | **262/262 PASS**（首跑 261/262：`test-version-contract.js` 首测试因 9f7738f 引入 v3 联合后失配 → 本轮修复为 v1/v2/v3 联合断言，重跑全绿） |
| mcp `npm test` | **52/52 PASS**（51 基线 + data-version-contract） |
| mcp `npm run check`（node --check + tsc --noEmit） | PASS |
| mcp `npm run check:openapi` | PASS（14 工具定义一致） |
| compiler `build-agent-prompts.js --check` | PASS（4 files） |
| `sync-campusflow-function.js --check` | PASS（11 files 一致） |
| `test-http-function.js` | PASS（health 200、13 Agent Tool Façade、V3 runtime） |
| Golden `eval-golden.js` | **33/33 PASS**，dataVersion=competition-demo-v1，dataHash=sha1:fefef4bf425b，verified=100% |
| 顶层 `npm test --prefix competition/adp-kit` | 全链 PASS，**唯一基线失败**：末尾 `sync-assets-manifest.js --check`（B 类 pre-existing，见 §7） |
| `git diff --check` | 干净（仅 LF/CRLF 警告，非错误） |

## 6. 4 个 final Prompt 未修改

`git diff 19da961..HEAD -- competition/adp-kit/r50.1/prompts` = 空。R50.1.1 对 `r50.1/` 目录仅改动 CUTOVER + ACCEPTANCE 两个文档（`git diff --name-only 19da961..HEAD -- r50.1` 仅此 2 文件）。

## 7. submission-package / manifest 审计（B 类 pre-existing，不混入本轮）

- `submission-package` 是生成产物：`build-submission-package.js`（唯一生成器，rm -rf 后重建 + 重算 SHA256SUMS）。已运行生成器验证：**build PASS（57 files）+ validate PASS（findings=0）+ 内嵌 golden-results/oracleSourceSha256 自洽**。
- 生成器产物 diff（+2054 行）全部可归因于 R49/R50 时代已提交文件的未同步快照：`git diff 19da961..HEAD -- competition/submission-package` = **空**（远程 19 commits 零改动提交包）。因此 mirror `contracts.ts` 仍为 v1|v2 是 pre-existing 状态，**非 R50.1.1 引入**；评审包锚定 competition-demo-v1 + 已提交时点源码，v3 类型声明不改变评审包行为。生成后已 `git checkout` + 删除 untracked 还原，避免 2054 行 pre-existing diff 混入本轮提交。
- `sync-assets-manifest.js --check` 基线失败：generated-assets-manifest.json 自 R49 时代未同步（R50/R50.1 已提交文件全部为 NEW/DIFF，如 v3 数据、r49-ma 新测试、r50.1 文档、temporal/ranking-core）。19da961..HEAD 对 manifest 零改动 → B 类，非本轮引入；按 R50.1 先例保留失败证据，不静默修复。
- 结论：R50.1.1 不修改 submission-package 与 manifest；如需评审包更新，属独立任务（R49.4.1 同款先例：快照保持）。

## 8. CI（PR #49 @ headRefOid=d462031）

全部 5 个 checks **SUCCESS**（读取真实结论）：

| Workflow | Check | Conclusion |
|---|---|---|
| Xiaofu Agent CI | agent-release-gate | SUCCESS |
| Competition ADP Widget CI | widget-contract | SUCCESS |
| Public Security Gate | public-security（×2 runs） | SUCCESS |
| Admin CI | admin-checks | SUCCESS |

push 后 CI 将针对新 HEAD 重跑。

## 9. 本轮本地改动（唯一提交）

- `competition/adp-kit/r49-ma/tests/test-version-contract.js`：首测试断言更新为 `CompetitionDataVersion = "competition-demo-v1" | "competition-demo-v2" | "competition-demo-v3"` + `DATA_VERSIONS` 三值数组（保持原语义：联合 + 默认 v1 保留 + Evidence 类型断言不弱化）。这是修复 ChatGPT `9f7738f` 引入的真实回归（原断言匹配 v1|v2 精确字符串）。

## 10. 约束核对

- 未改 CampusTools 业务算法、13 工具 HTTP Façade、endpoint/token、competition-demo-v3 内容、4 个 final Prompt 语义 ✓
- 未新增第 14 个工具；未触碰 Widget / avatar / Model A/B ✓
- 未部署 CloudBase（本轮无部署授权）✓
- 未创建自引用回填提交 ✓

## 11. 待办（用户动作，未变）

- 控制台真机验收 D1~D6（`r50.1/R50.1-CONSOLE-ACCEPTANCE.md`）：Agent Cutover（13 工具绑定按 §3 规范表）+ 4 个 final Prompt 粘贴。
- 通过后才可宣布 `R50.1.1 GOLDEN`（含 Console）；仓库侧判定见 §12。
- R50.2（Widget 增强等）未启动。

## 12. 仓库判定

`R50.1.1 REPO GOLDEN`：repo + CI 全绿，判定成立（唯一失败项为已分类的 B 类基线，保留证据）。Console GOLDEN 仍需 D1~D6 真机证据。
