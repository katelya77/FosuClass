# 2026-08-17 — R49.4.1 ADP 控制台验收模板（Console Acceptance）

> 本文件是 R49.4.1 的**控制台人工验收模板**（用户 + ChatGPT 执行，Agent 不代跑控制台）。
> 前置事实（2026-08-17 已确认，仓库侧）：CloudBase `/health` = `tools=9, agentTools=7, adpContractVersion=R49.4`；
> 数据 `dataVersion=competition-demo-v2`、`dataHash=sha1:4f3bbbb45d1f`；本轮**不重部署、不改运行时**。
> 参考：`R49-MA-ADP-MANUAL-CONFIG-CHECKLIST.md`（逐字段配置）、`09-MULTI-AGENT-E2E-MATRIX.md`（验收矩阵）。

---

## 0. 升级路径（二选一，推荐 A）

### 路径 A（推荐）：升级既有 5 工具插件 → 增量导入 delta JSON

1. ADP 控制台进入既有 CampusTools 自定义插件（5 个 operation）。
2. **增量导入** `competition/adp-kit/r49-ma/tools/openapi/campus-agent-tools.r49.4-existing-plugin-additions.json`
   （恰 2 个 operation：`campus_teacher_load_query`、`campus_schedule_range_query`，与全量文件同一 server、$ref 自包含）。
3. 导入后验证插件内 **7 个 operation** 全部存在（5 既有 + 2 新增），请求体/响应体 Schema 完整。
4. 保存插件；回填真实 PluginID / Endpoint / Token（**不得把 Token 写入仓库或截图**）。
5. 在「多智能体配置」更新工具绑定（与 R49.4 一致）：

| Agent | 工具绑定 |
|---|---|
| Main | `KnowledgeRetrievalAnswer` only |
| Schedule | `campus_schedule_query`、`campus_schedule_range_query`、`campus_classroom_search` |
| Risk | `campus_risk_check`、`campus_day_plan` |
| Insight | `campus_overview`、`campus_teacher_load_query` |

6. **重新粘贴 4 份 Prompt**（R49.4.1 source-aware 版本，逐字导入，不要增量追加）：
   `agents/main-orchestrator.md` / `campus-insight.md` / `schedule-space.md` / `risk-planning.md`。
7. 两个新工具的「工具调用结果直接返回给用户」= **OFF**（第一阶段文本全链验证）。

### 路径 B（仅当路径 A 的 delta 导入被控制台拒绝时）：新建临时插件全量导入

- 新建临时自定义插件，全量导入 `campus-agent-tools.adp-import.json`（7 operations），验证后绑定；
- **禁止先删除既有插件**：先导入新插件 → 验证 7 tools → 再切换绑定 → 确认无误后才可停用旧插件（保留回滚）。

---

## 1. 插件侧验收（Gate 前置条件）

| 检查项 | 预期 | 实测（截图/证据） | PASS/FAIL |
|---|---|---|---|
| 插件 operation 数（路径 A 增量 / 路径 B 全量） | 7 | | |
| `campus_teacher_load_query` 请求 Schema（weekStart/weekEnd 必填、topN/campus 可选） | 存在且可见 | | |
| `campus_schedule_range_query` 请求 Schema（entityType/entityName/weekStart/weekEnd 必填） | 存在且可见 | | |
| 两个新工具「结果直接返回用户」 | OFF | | |
| 4 份 Prompt 已逐字重贴（R49.4.1） | 是 | | |
| 工具绑定（Schedule +range、Insight +teacher_load） | 是 | | |

---

## 2. D1~D5 验收（应用首页，非单工作流调试）

> 每轮必须记录：route / state / inherited / dropped / tools / 实际工具调用（HTTP 证据：请求截图或服务日志）。
> evidence 列为空 → 该轮**不通过**。

### D1：未来四周教师负载最高的是谁

| 轮 | 输入 | 预期 route | 预期 tools | 预期 rankContext | 实际工具调用证据 | PASS/FAIL |
|---|---|---|---|---|---|---|
| 1 | 未来四周教师负载最高的是谁 | insight | campus_teacher_load_query(weekStart=1, weekEnd=4) | sourceTool=campus_teacher_load_query, selectedRank=null | 观察 fresh POST /api/campus_teacher_load_query body {weekStart:1,weekEnd:4} | |

预期回复要点：教师009与教师011并列最高（27/54 与 27/54）。按当前稳定排序 Top1=教师009、Top2=教师011。

### D2：看Top1课表

| 轮 | 输入 | 预期 route | 预期 tools | 预期 state | 实际工具调用证据 | PASS/FAIL |
|---|---|---|---|---|---|---|
| 2 | 看Top1课表 | schedule | campus_schedule_range_query(entity=排名工具结果[0], 1, 4) | entity=Top1(真实), rank=1, detailWindow={1,4}, NO_CLARIFICATION | 观察 fresh POST /api/campus_schedule_range_query；返回逐周展开 | |

**绝不**弹出「教师009/教师011/两位都看」选择；绝不传 week=4（聚合窗口污染）。

### D3：只看第一周

| 轮 | 输入 | 预期 route | 预期 tools | 预期 state | 实际工具调用证据 | PASS/FAIL |
|---|---|---|---|---|---|---|
| 3 | 只看第一周 | schedule | campus_schedule_query(entity=排名工具结果[0], week=1) | detailWindow={1,1}, academicWeek=1 | 观察 fresh POST /api/campus_schedule_query body {week:1} | |

### D4：新会话单句链

| 轮 | 输入 | 预期 route | 预期 tools | 预期 state | 实际工具调用证据 | PASS/FAIL |
|---|---|---|---|---|---|---|
| 1 | 看未来第一周课表负载最高的教师课表（新会话首句） | insight→schedule | campus_teacher_load_query(weekStart=1, weekEnd=1) → campus_schedule_query(entity=排名工具结果[0], week=1) | rankingWindow={1,1} → detailWindow={1,1}；无 KnowledgeRetrievalAnswer 兜底、无模型计数、无 Main↔Insight 循环 | 两条 fresh POST（teacher_load + schedule_query） | |

### D5：检查Top1风险（未给时间）

| 轮 | 输入 | 预期 route | 预期 tools | 预期 state | 实际工具调用证据 | PASS/FAIL |
|---|---|---|---|---|---|---|
| 4 | 检查Top1风险 | clarify | —（不调任何工具） | comparisonMode=self, entity=Top1, rank=1, pending=time_window | 无 risk 调用；Main 澄清时间窗口 | |

**绝不**静默 week=1 调 campus_risk_check；多周窗口（1..4）不是有效 risk week。

---

## 3. Console Gate（R49.4-GOLDEN 判定）

- [ ] 插件侧实测展示 **7 个 CampusTools**（截图）。
- [ ] D1~D5 每轮都有真实工具调用证据（HTTP 请求截图/日志）且与预期 tools 一致。
- [ ] D2/D3/D4 无澄清误弹、无 week=4 污染、无 Knowledge 兜底/循环。
- [ ] 13 核心 case + R48 A~G 无倒退（抽查 2 组即可，重点 D 链）。

以上全部 ✅ 且经用户核对后才允许声明「R49.4-GOLDEN PASS」；任一缺失 → 状态为「R49.4.1 仓库收敛完成，控制台待复测」，不得宣布完成。

---

## 4. 结论与回滚

- 结论：□ 全部通过（GOLDEN）　□ 部分失败（列失败 case）　□ 未执行（待用户排期）
- 失败 case 记录（case / 现象 / 期望 / 截图）：

| case | 现象 | 期望 | 截图/证据 |
|---|---|---|---|
| | | | |

- 回滚路径：
  1. 插件：若路径 A 增量导入异常 → 移除 delta 导入的 2 个 operation（或改走路径 B 全量临时插件），恢复 5 工具状态；
  2. Prompt：4 份 Prompt 重新粘贴回 R49.4 版本（或仓库上一提交版本 `fff35f0` 之前的文件内容）；
  3. 绑定：Schedule/Insight 工具绑定撤回至 R49.4 前状态；
  4. 运行时无需回滚（本轮未改 CloudBase / 未重部署）。