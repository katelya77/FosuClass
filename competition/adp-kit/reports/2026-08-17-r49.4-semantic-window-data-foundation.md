# R49.4 Semantic Window & Multi-School Anonymous Data Foundation — 交付报告

日期：2026-08-17 +08:00
分支：`feat/campusflow-adp-integration`（PR #49 全程 OPEN / UNMERGED）
执行依据：`docs/superpowers/plans/2026-08-17-r49.4-multischool-data-foundation.md`（Task 1→10，TDD）+ `2026-08-17-r49.4-opencode-execution-brief.md`

## 1. Task 1~10 实际状态

| Task | 内容 | 状态 | 证据 |
|---|---|---|---|
| T1 | 前端 Runtime 语义（CASE D 实景链接） | ✅ | `test-r49-4-semantic-window.js` + `multi-turn-cases.json`（D1→D2→D3→D5 链） |
| T2 | 新教师负载查询工具 `query_teacher_load` | ✅ | base tools 7→9；`test-adp-http-boundary.js` 扩展 |
| T3 | 新范围课表工具 `query_schedule_range` | ✅ | 多周/单周 academicWeek 语义；schema 对齐 |
| T4 | OpenAPI / 导入文件 7 operations | ✅ | `tools/openapi/campus-agent-tools.adp-import.json` 实测 7 paths（见 §5） |
| T5 | 契约与运行时版本 R49.4 | ✅ | `adpContractVersion=R49.4`；已有 5 façade 向后兼容 |
| T6 | CloudFunction 同步 + 本地冒烟 | ✅ | `sync --check` 8 文件一致；`test-http-function.js` health 200 / tools=9 / 7 façades |
| T7 | CASE D 重写 D1~D5 + 窗口链回归 | ✅ | 报告模板、09 矩阵、drilldown-rank-semantics / fixtures-consistency / g1-hardening 同步；147/147 |
| T8 | Data Hub foundation（匿名） | ✅ | `r49-ma/data-hub/*` + synthetic fixtures + 8 契约测试 |
| T9 | 隐私/匿名边界 gate | ✅ | `test-r49-4-anonymity-boundary.js`（7 契约）+ privacy-policy + builder 守卫；162/162 |
| T10 | 文档/清单 + 部署 | ⏳ 部分 | 清单/06/checkpoint 已更新；**CloudBase 部署被凭据阻塞**（见 §7） |

## 2. Semantic Window 最终规则

- `windowContext = { rankingWindow, detailWindow, academicWeek }`；`overviewWindow.count` 与 `academicWeek` 严格隔离。
- 「未来四周教师负载最高」→ `rankingWindow=1..4` → `campus_teacher_load_query(weekStart=1, weekEnd=4)`。
- 「看Top1课表」→ `detailWindow=1..4`（继承 rankingWindow）→ `campus_schedule_range_query`，**不得**默认 week=1、**不得**误成 week=4。
- 「只看第一周」→ `detailWindow=1..1` → fresh `campus_schedule_query(week=1)`（fresh-call 铁律）。
- 「看未来第一周课表负载最高的教师课表」→ teacher-load W1..W1 → stable Top1 → schedule W1..W1；严禁知识库回答/模型数课/Main↔Insight 死循环。
- 四周排名后「检查Top1风险」未指定时间 → 当前无 range-risk 工具 → **Main 澄清时间窗口，禁止静默 week=1**。
- 多周范围课表结果 items 带 `academicWeek` 字段；单周 fresh 调用结果无窗口污染。
- 排位：Top1/Top2/Top3 = 有序列表 position 语义；并列不澄清（如「教师009与教师011并列最高。按当前稳定排序，Top1=教师009，Top2=教师011」）。

## 3. `query_teacher_load` 实现与验证

- 实现：`mcp/campus-tools-mcp/src/tools.js`（权威源，仅此一份；cloudfunctions 副本经 `sync-campusflow-function.js` 生成）。
- 语义：多周窗口聚合 lessonOccurrences / periodUnits，稳定排序（DESC → periodUnits DESC → teacherName zh-CN tie-break）；`topN` 缺省 Top3；weekStart>weekEnd 或越界 1..20 → INVALID_PARAM fail-closed；未知 campus → fail-closed。
- 验证：本地 `node --test r49-ma/tests/*.js` 覆盖（窗口聚合、并列稳定、参数校验、campus 别名/未知校区）；`/health` 本地 tools=9。

## 4. `query_schedule_range` 实现与验证

- 实现：`mcp/campus-tools-mcp/src/tools.js`（权威源）。
- 语义：entity 多周窗口返回范围课表；每项带 `academicWeek`；周次表达式 1..20 校验（`week-expression.js` 同源解析逻辑）；entityType/entityName 必填，weekday/periodStart/periodEnd 可选；空结果/非法参数 fail-closed。
- 验证：本地 r49-ma 全量 + mcp 51/51（含范围查询新用例）；schema `agent-tools.json` 与 OpenAPI 对齐。

## 5. 7 个 Agent Tool 最终清单（ADP Façade）

1. `campus_schedule_query`（原）
2. `campus_classroom_search`（原）
3. `campus_risk_check`（原）
4. `campus_day_plan`（原）
5. `campus_overview`（原）
6. `campus_teacher_load_query`（新，Insight）
7. `campus_schedule_range_query`（新，Schedule）

`campus-agent-tools.adp-import.json` 实测 7 paths，tags=campus_schedule/campus_risk/campus_insight；OpenAPI `check:openapi` 与 9 个底层工具定义一致（1 个共享 spec）。

## 6. `/health` 本地结果（test-http-function.js 冒烟）

```
[pass] CloudBase HTTP Function 本地冒烟通过（health=200、确定性工具=9、7 Agent Tool Façade）
adpContractVersion=R49.4 / dataVersion=competition-demo-v2 / dataHash=sha1:4f3bbbb45d1f
```

## 7. CloudBase 部署与远程 `/health` — ✅ 已部署（5 项鉴权复验待用户提供 token）

- **部署已完成（2026-08-17，用户 `tcb login` 后）**：`tcb fn deploy campusflowAdpTools --dir competition/adp-kit/cloudfunctions/campusflowAdpTools --httpFn --path /campusflow-adp-tools --force`（临时在根 `cloudbaserc.json` 追加函数条目以跳过交互；**未写 envVariables**，线上 CAMPUS_API_TOKEN 等环境变量保持不变；部署后已从备份逐字节还原 cloudbaserc.json，`git status` 干净）。中途一次失败：PS 5.1 `Set-Content -Encoding UTF8` 写入 BOM 导致 CLI JSON 解析失败 → 改用无 BOM 写入后成功。
- 新公网入口：`https://cloud1-d3g17rpe7566d3d5c.service.tcloudbase.com/campusflow-adp-tools`（原 `.app.tcloudbase.com` 旧域名入口仍可用）。
- **远程 `/health` 实测（无鉴权 GET）= 目标值完全一致**：
  ```json
  {"status":"ok","service":"campus-tools-mcp","version":"1.0.0","dataVersion":"competition-demo-v2","dataHash":"sha1:4f3bbbb45d1f","tools":9,"agentTools":7,"adpContractVersion":"R49.4"}
  ```
- **环境变量保全证据**：`index.js` 缺 `CAMPUS_API_TOKEN` 会拒绝启动（README 契约）→ 部署后函数正常冷启动并返回 health 200（uptime 11s）→ 线上 token 环境变量未被覆盖；`tcb fn detail` 显示 `CAMPUS_API_AUTH_MODE=token` 与 `CAMPUS_API_TOKEN` 均在场（CLI 对值做了掩码显示）。
- **鉴权未倒退**：无 token POST `/api/campus_risk_check` → 401；伪造 token → 401（token 模式仍在强制）。
- **剩余 blocker（不伪造）**：5 项鉴权复验需要真实 `CAMPUS_API_TOKEN` 值；`tcb fn detail` 输出掩码（`AAAA…`），本会话环境无该变量，且纪律禁止自动化登录/扩权/读取无关项目 .env（README 明确 token 只存 CloudBase 函数环境变量与 ADP 环境变量）。→ 需用户在会话环境提供 token（如 `setx CAMPUS_API_TOKEN <值>` 后告知），或自行执行下方脚本。

## 8. 两个新远程 façade 实测摘要 — 部署后待补录（鉴权 token blocker）

计划复验（与 brief 一致）：① teacher load W1..W1；② teacher load W1..W4；③ Top1 schedule range W1..W4（items 带 academicWeek）；④ T09 week1 self risk = conflictCount=1 / rushWarningCount=1（基线不倒退）；⑤ 原 schedule/classroom/day_plan/overview 不倒退。

用户可自行执行的复验脚本（粘贴 token 到变量，输出不含 token）：

```powershell
$base = "https://cloud1-d3g17rpe7566d3d5c.service.tcloudbase.com/campusflow-adp-tools"
$env:CAMPUS_API_TOKEN = "<粘贴真实 token>"
$h = @{ Authorization = "Bearer $env:CAMPUS_API_TOKEN" }
function Post-Api($n, $b) { Invoke-RestMethod -Uri "$base/api/$n" -Method Post -Headers $h -Body (($b | ConvertTo-Json -Compress) -replace '"','\\"') -ContentType "application/json; charset=utf-8" }
# 1) teacher load W1..W1；2) W1..W4
Post-Api "campus_teacher_load_query" @{weekStart=1;weekEnd=1} | ConvertTo-Json -Depth 6
Post-Api "campus_teacher_load_query" @{weekStart=1;weekEnd=4} | ConvertTo-Json -Depth 6
# 3) Top1 schedule range W1..W4（items 应带 academicWeek）
Post-Api "campus_schedule_range_query" @{entityType="teacher";entityName="教师009";weekStart=1;weekEnd=4} | ConvertTo-Json -Depth 6
# 4) T09 week1 self risk 基线
Post-Api "campus_risk_check" @{mode="self";entityType="teacher";entityName="教师009";week=1} | ConvertTo-Json -Depth 6
# 5) 旧接口回归
Post-Api "campus_schedule_query" @{entityType="teacher";entityName="教师009";week=1;weekday=5} | ConvertTo-Json -Depth 6
Post-Api "campus_classroom_search" @{campus="校区A";week=1;weekday=1;periodStart=1;periodEnd=2;minCapacity=60} | ConvertTo-Json -Depth 6
Post-Api "campus_day_plan" @{date="2026-09-05"} | ConvertTo-Json -Depth 6
Post-Api "campus_overview" @{} | ConvertTo-Json -Depth 6
```

## 9. 旧接口回归结果

- 本地：mcp 51/51（原 5 接口 + 新 2 接口全量回归）；r49-ma 162/162（A~H + 13 core + 窗口语义 + 数据枢纽 + 隐私边界）；`sync --check` 8 文件一致；adp-kit 全链 `npm test` 通过（唯一基线失败=用户未提交的 `generated-assets-manifest.json` 漂移导致的末步 `sync-assets-manifest --check` 不一致，改动前已存在；跑前已备份、跑后逐字节还原用户漂移）。
- 远程：待部署（§7）。

## 10. R49-MA 全测试数量与结果

- `node --test "competition/adp-kit/r49-ma/tests/*.js"` = **162/162**（T1~T9 每步红→绿，无弱化旧断言；A~H/13 core/R48 断言保留）。

## 11. MCP / HTTP / OpenAPI / Golden 结果

- mcp `npm test` = **51/51**；`run check`（tsc）= 0 错误；`run typecheck` = 0 错误；`run check:openapi` = [pass]。
- cloudfunctions：`sync-campusflow-function.js --check` = [pass]（8 files 一致）；`test-http-function.js` = [pass]。
- Golden：`eval:golden` 33/33（本轮 tools.js 未改动，oracleSourceSha256 不变）；`npm test --prefix competition/adp-kit` 其余链（widget CI / submission 扫描 54 文件 0 findings / artifact 校验）通过。

## 12. Data Hub foundation 文件与 week expression 支持

- 文件：`r49-ma/data-hub/{canonical-schema.json, week-expression.js, validator.js, source-detector.js, provider-capabilities.json, privacy-policy.json, adapters/{base-adapter.js, portal-family-qz-legacy.js}}`；`r49-ma/tests/fixtures/synthetic-legacy-timetable.json`；`r49-ma/tests/test-r49-4-data-hub.js`（8 契约）。
- week expression 支持：`1-16周`（连续区间）、`1-8周(单)` / `2-8周(双)`（单双周）、`第2,4,6周`（离散列表）；范围 1..20、单双冲突校验；适配器行级跳过 + fail-closed。
- 命名空间：`anonymous` / `synthetic-example` / `competition-demo-v2`；provider family：`portal-family-qz-legacy` / `xlsx-generic` / `csv-generic`；capability：credentialed=false、rawImportAllowed=false、anonymous-only。

## 13. 隐私扫描结果（真实数据零提交确认）

- `test-r49-4-anonymity-boundary.js`（7 契约）按 builder 的 `submissionInputFiles()` 接受过滤器全量扫描 submission-package + r49-ma：真实学校身份词（`佛山大学/佛山市/佛山科学技术学院/katelya/思囿`）仅存在于 3 个 gate 校验器文件（validate-competition-demo-v1/v2、validate-submission-package、eval 数据集/golden）的 deny-list 中（`GATE_FILES_BY_PATH` 豁免），绝不复制进生成的比赛资产；fixture 全为中性值（教师A01–A03、2026级示例1~3班、示例课程A–D、校区A/B/C、A1-101…）。
- 凭据/个人模式：quoted 值密码/Cookie/Token/secret/sessionId + dummy 值豁免 + 必填 padding 的 base64；`*.xls/*.xlsx` 扩展名、个人文件名模式（`*课表*`、`\d{6,}` 学号等）全部拒绝。
- `build-submission-package.js` 新增 `PERSONAL_IMPORT_EXCLUDED = ["personal-import","raw-import","private-import","personal-timetables"]` 目录硬守卫（存在即 throw）。
- 结论：**未提交任何真实 .xls / 学校 / 个人数据**；grep 全仓确认真实学校名仅存于 gate deny-list。

## 14. 用户手工执行的 ADP 自定义插件更新步骤

1. ADP 控制台 → 自定义插件 → 更新 CampusTools 插件：导入 `r49-ma/tools/openapi/campus-agent-tools.adp-import.json`（**必须 7 operations**，见 §5）。
2. 按 `R49-MA-ADP-MANUAL-CONFIG-CHECKLIST.md` §8.1 配置两个新工具参数可见性（teacher_load：weekStart/weekEnd 必填 ON、topN/campus 可选 ON；schedule_range：entityType/entityName/weekStart/weekEnd 必填 ON、weekday/periodStart/periodEnd 可选 ON）。
3. 所有工具「工具调用结果直接返回给用户」保持 **OFF**（Widget Direct Output 第二阶段再开）。
4. Schedule 新绑定 `campus_schedule_range_query`；Insight 新绑定 `campus_teacher_load_query`。
5. 应用首页按 D1~D5 复测 CASE D（见 `R49-MA-ADP-MANUAL-CONFIG-CHECKLIST.md` §6/§8.1 验证口诀）。
6. 确认回填真实 PluginID/AgentID（当前占位符 FAIL CLOSED）。

## 15. 需要整体替换的 Agent Prompt 文件（仓库真源，逐字导入）

- `competition/adp-kit/r49-ma/agents/main-orchestrator.md`（7 动态工具；WindowContext 窗口语义硬性要求；rankContext 跨域注记）
- `competition/adp-kit/r49-ma/agents/campus-insight.md`（Ranking Window 语义：窗口变化必须 fresh 负载查询）
- `competition/adp-kit/r49-ma/agents/schedule-space.md`（范围课表继承规则：多周→范围工具、单周→fresh 单周、无窗口→NEED_CLARIFICATION）
- `competition/adp-kit/r49-ma/agents/risk-planning.md`（显式周次/日期才下钻；未给时间→NEED_CLARIFICATION 绝不静默 week=1）

## 16. commit SHA / push 状态 / PR 状态 / 回滚点

- 本轮 commits（按序）：T7 `682b0a7`、T8 `791b6e9`、T9 `6380c9a`；T10 文档（checklist/06/checkpoint/本报告）为第 4 个 commit，待 push。
- push 状态：待用户确认后 `git push origin feat/campusflow-adp-integration`（推送允许；不经用户要求不 push）。
- PR #49：**OPEN / UNMERGED**（保持；不 merge、不发布正式应用）。
- 回滚点：如需回滚本轮 = `git reset --hard 6380c9a^`（T9 完成点）或按需 `git revert 6380c9a..HEAD`；`competition/submission-package` 与 `reports/generated-assets-manifest.json` 的既有用户漂移未触碰（跑套件前后已备份/还原验证）。
- 已知未验证项（真实 blocker，不伪造）：**5 项鉴权远程复验（§8）**——根因：`CAMPUS_API_TOKEN` 真实值不可在本会话取得（CLI 掩码显示、环境无变量、纪律禁止扩权/读无关项目 .env）；部署本身已 100% 完成并经 `/health` 验证（§7）。用户提供 token（或自行运行 §8 脚本）后即可补录。CloudBase 部署包与运行态已就绪：`/health` 实测 `tools=9 / agentTools=7 / adpContractVersion=R49.4`。
