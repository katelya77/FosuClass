# 校园智序 · 小序 — 当前 ADP 研发检查点

更新时间：2026-08-11 02:10 +08:00

## 基础能力已全部冻结

- 01 多维课表查询：真实 ADP 调试通过，冻结。
- 02 空教室规划 V7.2：基础查询、两轮继承、五轮累计槽位全部真实 ADP 通过，冻结。
- 03 课程冲突比较 V5.1：整周比较、自比较去伪冲突、跨校区赶场去重、单教师 self-compare、01→03 跨工作流 handoff 均真实 ADP 通过，冻结。
- 04 今日校园计划 V1.1：核心 5 条 + 边界 5 条真实 ADP 验收全部通过，冻结。

## 02 V7.2 最终真实验收

同一会话连续：

1. `校区A 2026-09-03 下午有哪些空教室` -> PASS：校区A + 2026-09-03 + 第5-8节。
2. `那校区B呢` -> PASS：只替换为校区B，日期与节次保留。
3. `要能坐60人的` -> PASS：校区B + 2026-09-03 + 第5-8节 + 容量>=60。
4. `改成第3-4节` -> PASS：校区B + 2026-09-03 + 第3-4节 + 容量>=60。
5. `只看B1楼` -> PASS：校区B + 2026-09-03 + 第3-4节 + B1 + 容量>=60；工具返回 verified=true 的空结果，未伪造教室。

因此三轮以上累计槽位问题正式关闭。

## 应用级路由与上下文已冻结

- 标准模式应用启用 01/02/03/04 四条工作流。
- 模型输入上下文改写已开启。
- 角色指令为 V2.1。
- 02 五轮累计上下文真实通过。
- 03 单教师赶场真实通过。
- 01→03 handoff 真实通过：`教师003第1周周一的课` -> `再和A班比较一下有没有冲突` 正确切换到 03，并保留教师003 + 第1周 + 周一。
- 不再继续修改 01–04 的确定性业务逻辑，除非后续基准评测发现回归。

## 已形成的 ADP 导入包生成永久规则

1. `Nodes[].NextNodeIDs` 与顶层 `Edge` 必须同时更新；
2. 所有 `REFERENCE_OUTPUT.Reference.NodeID` 必须存在；
3. 被引用节点必须位于消费节点真实上游路径；
4. START 到所有业务节点必须可达；
5. 能只改 workflow JSON 就不改已经通过 ADP 导入的 XLSX；
6. 参数提取输出、Tool 输出 Schema 和 NodeUI output 必须同时注册；
7. 顶层 parameters.xlsx 的 `ParameterParentId` 保持空值；
8. 每次生成 ZIP 都执行 CRC、节点可达性、引用上游性、XLSX/ID 不变量校验。

## 当前进入阶段：Widget 产品化

仓库已经存在 `competition/adp-kit/widget/`：

- `校园任务结果卡.md`：定义 schedule / classroom / conflict / day_plan / error / choice 六种 cardType；
- `widget-schema.json`：统一结构化数据合同；
- `widget.js` / `styles.css` / `index.html`：已有匿名 H5 演示实现；
- `sample-results.json/js` 与生成脚本：已有确定性样例资产。

腾讯云 ADP 当前 Widget 能力支持：

- Widget 节点展示结构化结果；
- `sys.chat` 将按钮/表单操作作为新用户输入继续对话；
- `sys.go_to_url` 做无需 Agent 感知的直接跳转；
- `sys.download` 下载；
- Widget 节点可选择直接向后流转或等待用户操作。

下一阶段优先把已经稳定的确定性工作流结果升级为原生交互式卡片，不复制 CampusTools 业务逻辑。

## 后续路线

1. Widget 四类正式主卡 + error/choice 辅助卡；
2. sys.chat 交互动作形成“查看整周 / 换校区 / 改容量 / 比较冲突 / 规划当天”等任务闭环；
3. 知识库 + 32 组标准 QA 精修、来源展示与 token 成本优化；
4. 80 条 ADP 原生基准评测 + 多提示词对比；
5. 应用级安全/匿名/提示注入测试；
6. 多模态输入；
7. 发布前 test release 与最终发布闸门。

PR #49 保持 open、未 merge、未正式发布。
