# 校园智序 · 小序 — 当前 ADP 研发检查点

更新时间：2026-08-11 02:05 +08:00

## 已冻结能力

- 01 多维课表查询：已通过真实 ADP 调试。
- 03 课程冲突比较 V5.1：整周比较、自比较去伪冲突、跨校区赶场去重、单教师 self-compare、01→03 跨工作流 handoff 均已通过真实 ADP 调试，正式冻结。
- 04 今日校园计划 V1.1：核心 5 条 + 边界 5 条真实 ADP 验收全部通过，正式冻结。

## 02 空教室规划当前状态

- V6/V6.1 基础查询与两轮上下文继承通过。
- V7/V5 首版曾因新增节点只更新 `NextNodeIDs`、漏更新顶层 `Edge`，导致新节点漂浮并触发“引用节点不存在”；该结构问题已在 V7.1/V5.1 修复，并形成永久生成规则：必须同时检查 NextNodeIDs、Edge、REFERENCE_OUTPUT、START 可达性和上游可达性。
- V7.1 真实应用测试结果：
  1. `校区A 2026-09-03 下午有哪些空教室` -> PASS；
  2. `那校区B呢` -> PASS，正确继承 2026-09-03 + 第5-8节；
  3. `要能坐60人的` -> FAIL，仍丢失日期/节次；
  4. `改成第3-4节` -> 虽成功，但出现校区从 B 回滚到 A，说明仅依赖 RewriteQuery/参数提取并不足以稳定承载三轮以上累计状态；
  5. `只看B1楼` -> 能恢复校区B与容量，但上下文来源仍不稳定。

### V7.2 修复策略

不再新增 APP 状态，不再改拓扑，不再依赖 RewriteQuery 必须完整累计。

`上下文参数合并` 改为：

1. 本轮 `SYS.UserQuery` 明确条件最高优先；
2. 从 `SYS.ChatHistory` 中读取最近一次由本工作流输出、且包含 `核验：已通过` 的稳定 `查询条件：...` 行，作为确定性累计基线；
3. RewriteQuery 只作为第三优先级补空；
4. 参数提取器输出最后兜底；
5. 本轮明确改节次时清除旧 `consecutive_periods`，防止旧连续节数重新覆盖新 end_period。

V7.2 本地验证：

- 只修改 workflow JSON；5 个 XLSX 与 V7.1 字节级一致；
- ZIP CRC PASS；
- 9/9 节点从 START 可达；
- 五轮累计确定性模拟 PASS：最终状态为 `校区B + 2026-09-03 + 第3-4节 + B1 + 容量>=60`；
- SHA256：`ba476d92a6d12d3ab198f71824107d3488a06ff1bb73652d125695de4c02c5ac`。

## 应用级路由与上下文

- 标准模式应用已启用 01/02/03/04 四条工作流。
- 模型输入上下文改写已开启。
- 角色指令为 V2.1。
- 03 单教师赶场与 01→03 handoff 已真实通过，说明跨工作流 handoff 层可冻结。
- 当前唯一未冻结的基础层能力是 02 三轮以上累计槽位。

## 下一步验收

导入 `02-空教室规划-V7.2-最近核验条件继承版-可直接导入.zip`，同一会话依次：

1. `校区A 2026-09-03 下午有哪些空教室`
2. `那校区B呢`
3. `要能坐60人的`
4. `改成第3-4节`
5. `只看B1楼`

目标：第3轮必须直接查询 `校区B + 2026-09-03 + 第5-8节 + 容量>=60`；第4轮仅替换节次；第5轮仅增加 B1 楼栋。

## 后续路线（上下文层一旦冻结立即执行）

1. Widget 四类正式卡片与 sys.chat 交互动作；
2. 知识库 + 32 组标准 QA 精修、来源展示与 token 成本优化；
3. 80 条 ADP 原生基准评测 + 多提示词对比；
4. 应用级安全/匿名/提示注入测试；
5. 多模态输入；
6. 发布前 test release 与最终发布闸门。

PR #49 保持 open、未 merge、未正式发布。
