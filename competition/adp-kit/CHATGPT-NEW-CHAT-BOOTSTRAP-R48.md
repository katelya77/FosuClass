# 新对话一键接管提示词 — 校园智序·小序 R48

把下面整段作为**新对话的第一条消息**发送即可：

---

你现在正式接管我在同一个 ChatGPT Project 中持续开发的 **FosuClass / 腾讯云智能 ADP 赛事智能体「校园智序·小序」**。这不是新项目，也不是重新设计，请直接继承已有工程状态并继续开发，不要让我重新复述历史。

## 你第一步必须做的事

请立即使用 **@GitHub** 读取仓库 `katelya77/FosuClass`，切到/读取分支 `feat/campusflow-adp-integration`，并完整阅读：

`competition/adp-kit/CHATGPT-HANDOFF-R48-APP-CONTEXT.md`

这是当前最高优先级的跨对话接管文档。若需要追溯历史，再读：

`competition/adp-kit/CHATGPT-HANDOFF-R47-APP-CONTEXT.md`

同时确认 PR #49 当前仍为 open / unmerged，且不要自动合并、不要自动发布正式生产。

**优先级规则：**我在本新对话里最新上传的 Tencent ADP 工作流 ZIP / Widget 导出 / error 标注文件 / 最新截图与 trace > R48 接管文档 > R47 接管文档 > 更早历史。不要用旧 schema 覆盖最新真实导出。

## 当前核心状态，你必须先接受这些结论

- CampusTools / CloudBase 已升级到 `competition-demo-v2`，线上 dataHash 为 `sha1:4f3bbbb45d1f`；不要回退 v1。
- 5 个核心任务：01 多维课表、02 空教室、03 冲突/赶场、04 今日计划、05 校园教学态势。
- 01～05 首轮基本都能跑；05 的“全部为 0”历史 bug 已修，当前可显示约 4 个教学周、220 次课程、12 位教师、36 间空间、3 校区等非零结果。
- 单个工作流调试中的多轮上下文基本正常。
- Widget 按钮 action / 跨工作流按钮目前实测基本正常。
- **真正仍未解决的是：应用首页正常聊天时，用户不用按钮、只输入很短的自然语言追问，顶层应用几乎没有稳定继承上一轮任务与参数。**

最新应用层实测证据包括：

1. `T03老师第1周周一有什么课` 首轮正常；紧接 `只看周三` 却重新追问实体类型/名称。
2. 课表 Widget 点 `查看整周` 正常，可得到真正整周课表，说明按钮链正常。
3. 空教室 Widget 的 `下一时段` / `换校区` 按钮正常；但自己输入 `大于60人的呢` 会重新追问校区。
4. `帮我安排2026年9月4日的一天` 首轮正常；自己输入 `下一天呢` 会进入“校园工具暂时不可用”恢复卡，而 04 工作流内部调试的“下一天”曾正常。
5. 05 Overview 当前非零；Widget `看Top1课表` 能钻取到本轮真实 Top1（最新截图为 T09），`检查Top1风险` 也能正确钻取。

因此请不要把问题继续归咎于 CampusTools，也不要为了修应用层文本上下文去重写已经稳定的 01/02/04 内部 context merge 或破坏 Widget action。

## 下一阶段正式命名与目标

下一阶段直接进入：

**R48 Application Context Bridge / Follow-up Resolver**

目标是在“应用首页正常聊天、不点 Widget 按钮”的情况下，稳定支持：

- `只看周三`
- `那整周呢`
- `再看周五`
- `下一周呢`
- `换到B校区`
- `改成第3-4节`
- `大于60人的呢`
- `下一天呢`
- `那有赶场风险吗`
- `再看看他的风险`

要求只覆盖用户本轮明确修改的 slot，其他已确认 slot 继承上一轮；若切换任务，可跨工作流携带兼容上下文；只有真实歧义才追问。动态校园事实仍必须最终由 01～05 / CampusTools 核验，模型不得脑补。

请优先调查腾讯云智能 ADP 当前平台真实支持的：conversation history、session/global variables、会话变量、Agent/工作流节点、变量赋值/聚合、顶层工作流选择是否能读取上一轮结果、Widget action payload 与用户自由文本消息的差异，以及是否可以在 5 个工作流前放一个统一 Follow-up Resolver。

实现优先级：

1. 平台原生 structured session state / 会话变量；
2. 统一 `00-应用上下文路由 / Follow-up Resolver`；
3. 如果平台确实缺少持久化，再考虑 CloudBase 轻量 session-state service；
4. prompt 补强只能做辅助，不能再次把“多加几句角色指令”当成根治。

不要把 5 个稳定工作流粗暴合成一个巨型工作流，除非有腾讯平台证据证明这是唯一稳定方案。

## 你的工作方式

- 深度思考，优先根因和一次验收成功率，不要快速给表面方案。
- 可以使用 @GitHub、网页搜索腾讯官方文档、当前对话附件、必要的工具进行核验。
- 如果新对话无法访问旧附件，只让我补传**最新一套**工作流 ZIP / Widget 导出 / error 文件，不要让我重新解释历史。
- 需要生成工作流/Widget 时，必须以“导入后再导出的真实 Tencent Widget identity/schema”为绑定真值，禁止猜 Widget ID。
- 每次交付必须说清：停用/删除哪些旧文件、导入哪些新文件、导入顺序、是否重绑 Widget、是否改角色指令/知识库，以及复验话术。
- 不要自动 merge PR #49，不要自动发布正式生产，不要提交 token/密钥/真实身份信息。
- 本地静态验证通过只能叫“本地验证通过”，必须等待我在 Tencent ADP 应用层真机复验后才能宣称最终通过。

## R48 完成后的最低应用层验收矩阵

A. 01：
`T03老师第1周周一有什么课` → `只看周三` → `那整周呢` → `再看周五` → `下一周呢` → `上一周呢`

B. 02：
`A校区第1周周一第5-6节有哪些40人以上的空教室` → `换到B校区` → `改成第7-8节` → `大于60人的呢` → `下一周同一时间呢`

C. 01→03：
`T03老师第1周周一有什么课` → `那有赶场风险吗`

D. 04：
`帮我安排2026年9月4日的一天` → `下一天呢` → `再下一天`

E. 05→01→03：
`从8月25日开始看看未来几周校园教学运行情况` → `看看Top1老师第1周课表` → `再看看他的风险`

F. 新会话无历史：第一句直接 `只看周三`，此时必须合理追问，不得伪造历史。

G. Widget 按钮回归：01 查看整周、02 下一时段/换校区、03 查看课表/找后续空教室、04 找空教室/下一天、05 看Top1课表/检查Top1风险都不能被 R48 改坏。

## 现在请直接开始

读取 GitHub R48 handoff 后，先用一段简洁但具体的文字告诉我：

1. 你已经继承到的当前真实状态；
2. 你认为应用层文本多轮为什么会在“工作流调试正常 / Widget 按钮正常”的同时失败；
3. R48 你准备采用的最稳妥架构与验证路径；
4. 然后直接进入后续研发，不要让我重新讲一遍项目。

---
