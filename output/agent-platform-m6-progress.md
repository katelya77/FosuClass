# Agent Platform M6 进度

| 时间（+08:00） | 任务 | 记录 |
|---|---|---|
| 2026-07-28 19:39:13 +08:00 | M6-T1 意图→工具映射单源化（toolRegistry 独占） | `server/src/services/ai/toolRegistry.js`（+116/−34）：`buildPlanForIntent` 改由 manifest 单源派生——新增 `resolveManifestAllowedTools`，规划候选集 = Intent（manifest `intent.allowedTools`）∩ Skill（`intent.skill` 指向的 manifest skill `allowedTools`）∩ Runtime（`TOOL_HANDLERS` 已注册可执行）∩ Principal（`context.principal` 显式存在且未认证时剔除 `PRINCIPAL_REQUIRED_TOOLS`：create/update/delete/list_course_reminder、update_user_preference）∩ Environment（`context.runtimeMode` 经 `isToolAllowedForRuntime`，缺省 public 最严口径），纯交集不并集；删除 legacy：generic/project_qa/conversational_help 早退特判与 `buildPlanStep(intent.name,…,"执行权威工具")` 身份兜底（默认分支改取交集首位工具）；各多步分支保留消息/槽位编排但逐步经交集过滤。收窄行为变化（逐条）：①`conversation_memory` 旧发幻影工具步骤（下游必被滤）→ 现 `[]`；②直接调用 `buildPlanForIntent` 且 context 无 runtimeMode 时 trial/dev 专属工具（generate_image）→ 现按 public 最严剔除（在线链路由 requestContextAssembler 注入 runtimeMode，kernel 在 public 先被 skill 门禁拦截，无功能断层）；③显式 `context.principal.authenticated===false` 时提醒/偏好工具被剔除（新收窄语义，未携带 principal 时维持执行层鉴权不变，登录/确认 UX 不受影响）。新测试 `tools/test-agent-tool-plan-manifest.js` 10 组（五因子红绿、并集放大反例锁定、fixture 子进程改 manifest 即改变计划的单源证据、空计划回退、形态兼容、全 manifest 不变量扫描）两轮全绿，被 regression 套件自动发现。验证：`node --check` 通过；四门门禁全绿（foundation 33/33、regression 125/125 含新测试自动发现、ai-competition、final-convergence）；test-agent-planner / test-ai-tool-chain / test-xiaofu-core-experience 及 grep 盘清的 14 个既有相关测试全绿。grep 证据：toolRegistry 内 `allowedTools` 仅出现于 manifest 派生函数，无第二份 intent→tool 映射。【疑似缺陷】无新增；遗留：T2（capabilityRouter 正则权重）、T3（kernel useToolChain 旧链）按 M6 串行计划后续处理。 |
| 2026-07-28 20:11:11 +08:00 | M6-T2 capabilityRouter 正则权重降级为 Manifest 派生兜底层 | `server/src/services/ai/capabilityRouter.js`（+107/−55，删手工映射约 39 行）：删除 `INTENT_HINT_WEIGHTS`（intent→skill 手工权重表）、`MESSAGE_HINTS` 内全部手工 `tools: [...]` 工具 id 列举、`ALWAYS_TOOLS` 手工工具表及个人课表 boost 手工三工具表；新增 `manifestToolsForIntent`（intent 候选 = manifest `intent.allowedTools` ∩ 对应 skill `allowedTools` ∩ `toolRegistry.listToolNames()` 已注册 ∩ `isToolAllowedForRuntime`，与 T1 `resolveManifestAllowedTools` 同口径）与 `manifestRoutableToolSet`（全部 intent 映射并集，skill 扩展路径工具亦须落入并集，真实 manifest 两集合相等故零行为变化）；兜底层正则命中只产出 intent 名（`MESSAGE_FALLBACK_HINTS.intents` / `BASELINE_INTENTS`），工具一律经 manifest 派生交集获得，不旁路 T1（Principal 因子在本层惰性：路由输入经 `sanitizeAgentContext` 白名单不含 principal，计划/执行层由 `buildPlanForIntent` 与工具鉴权再收口）。收窄/变化逐条：①INTENT_HINT_WEIGHTS 删除致 skill 并列位次漂移——多目标消息第 3 skill 由 campus_multi_step_advice 变 campus_weather（少 search_campus_place 候选）、提醒/冲突消息第 3 skill 变 course_action_advice（多 get_next_course/get_course_route 候选，均在 manifest 映射内）；②提醒兜底由 create+list 扩为 manage_course_reminders 全 intent 允许集（update/delete 本已随 skill 入选，净增仅位次）；③冲突兜底新增 navigate_miniprogram_page（manifest intent 内）；④个人课表 boost 由手工 3 工具扩为 `needsPersonalScheduleSummary` 意图主工具（+get_week_schedule/recommend_meeting_time/get_course_weather_advice，均 manifest 内）。测试：`tools/test-agent-tool-plan-manifest.js` 追加 G11（兜底定位+形态+源码无手工表断言）/G12（fixture 子进程改 manifest 即改变 router 产出、ghost 工具剔除、skill 因子不旁路）/G13（8 输入×public/trial 不变量扫描：候选 ⊆ 已注册 ∩ runtime 政策 ∩ ⋃intent.allowedTools，且 `buildPlanForIntent` 计划 ⊆ router 候选），13/13 两轮全绿。验证：`node --check` 双文件通过；test-agent-memory-autonomy（grep 唯一既有 router 测试）/test-agent-model-first-understanding/test-xiaofu-core-experience(72/72)/test-agent-planner 两轮全绿；grep 证据：capabilityRouter 无 `tools: [` 列举，工具命名空间字符串仅存于 intent 名位置。【疑似缺陷】无新增；遗留：T3（kernel useToolChain 旧链）、T4（durable）按 M6 串行计划后续处理。 |
| 2026-07-28 20:41:51 +08:00 | M6-T3 kernel 双执行架构收敛为单一 PlannerCoordinator 路径 + MAX_REPLAN 文档口径修正 | `server/src/services/ai/agentKernel.js`（+18/−107）：删除 `useToolChain` 旧链全部分支——constructor 的 `toolChainExecutor`/`useToolChain` 选项与注释（3 行）、`executeToolChain` 方法（52 行）、`execute()` 内旧链分派块（37 行，`useToolChain && !forcePlanner`），所有执行统一走 PlannerCoordinator→kernel→ObservationLoop 新链（旧链死代码证据：全仓 grep 无任何 `useToolChain:true`/`toolChainExecutor`/`forcePlanner` 调用方，AgentKernel 仅 defaultKernel 与测试构造且均不传该选项；新链返回字段为旧链超集，`PLAN_STEP_LIMIT_EXCEEDED`/`TOOL_NOT_ALLOWED_FOR_SKILL` 门槛前置到计划校验，行为等价或收窄）；业务恢复逻辑（原 `executePlan` 内硬编码 168-179 区间）下放 Skill 定义——`server/src/services/ai/skillRegistry.js`（+20/−0）新增 `SKILL_RECOVERY_RULES` 声明表（`find_empty_room`/`find_continuous_empty_room`/`recommend_meeting_time`/`campus_multi_step_advice` 四个 skill，`attachResult` 侧挂 `emptyRoomResult→search_empty_rooms`、`callOnEmptyOrFailure` 空结果/失败追加 `diagnose_data_status`），经 `createSkill` 挂 `skill.recoveryRules`，kernel 只保留按规则匹配的通用执行机制（5 组行为等价自检通过：会议侧挂/空教室诊断/失败诊断/无规则 skill 零副作用/自定义 registry 无 recoveryRules 不炸）；`executePlan` 经 `Object.assign({}, input, { skill })` 接收 skill，签名兼容。MAX_REPLAN 口径（主 Agent 裁定代码现实为准 ≤2）：`AGENTS.md` 83 行「最多 1 次」→「最多 2 次（以代码现实为准：实际生效上限为 planSchema.MAX_REPLAN = 2）」，其余不动；`server/src/services/ai/planner/plannerPolicy.js`（+3/−1）22 行加注释注明生效上限在 planSchema 且该字段无消费方，clamp `min(1)`→`min(2)` 对齐（全仓 grep 证实 `policy.maxReplan` 零消费方，行为中性）；`planSchema.js` 常量未动。`runtime/plannerCoordinator.js` 无需适配（本就不引用 useToolChain）。收窄说明：跨 skill 候选场景下（主 skill 声明表外的 skill 经 router 候选运行空教室工具）不再触发诊断追加——真实路由中三 skill 声明已全覆盖 manifest 内可运行路径，无功能断层。验证：`node --check` 三文件通过；四门门禁全绿（foundation 33/33、regression 125/125、ai-competition、final-convergence 均 exit=0）；test-agent-kernel-foundation/test-agent-observation-loop/test-agent-planner/test-xiaofu-core-experience(72/72)/test-agent-e2e-matrix(5 场景） 及 grep 盘清的 test-agent-memory-autonomy(28)/test-ai-tool-chain/test-agent-tool-plan-manifest(13/13)/test-agent-run-events/test-ai-agent-tool-routing/test-planner-model-adapter/test-planner-model-adapter-http 全绿。grep 证据：kernel 内 `useToolChain|toolChainExecutor|executeToolChain|forcePlanner` 零命中（`runToolChainForIntentAsync` 仅存于冻结的 toolRegistry 自身导出）；kernel 内无 `recommend_meeting_time|search_empty_rooms|diagnose_data_status|emptyRoomResult` 业务字符串，恢复声明位于 skillRegistry.js:4-20。【不可覆盖点】无；【疑似缺陷】无新增；既有测试零改动（无旧链专属断言失效）；遗留：T4（durable）按 M6 串行计划后续处理。 |
| 2026-07-28 21:15:24 +08:00 | M6-T4 轻量 Durable Execution 层（提醒/审批/等待回执任务化） | 新增 `server/src/services/ai/durable/`（taskStore.js / waitForEvent.js / resume.js）：step-like 持久任务 = { taskId(dt_+randomBytes12), kind: reminder/approval/receipt_wait, status: pending/waiting/resumed/done/expired, waitEvent, resumeTokenHash(SHA-256), principalKeyHash(SHA-256), expiresAt(必填), context(裁剪), resumeCount, ... }；存储选型沿用服务端 JSON 文件惯例（参照 providerRuntimeConfigStore 的 FOSU_AI_PROVIDER_CONFIG_PATH 模式裁定 `FOSU_AI_DURABLE_STORE_PATH` > FOSU_DATA_DIR/ai/durable-tasks.json），单进程原子写(tmp+rename)从简无跨进程锁，resumeToken 由 crypto.randomBytes(24) 生成且只存哈希（明文仅登记时返回一次，不入存储/日志/响应），principal 只存哈希（proactiveCooldownStore 同口径）；状态机 create→pending→(arm)waiting→(resumeToken+principal+未过期校验)resumed→done，惰性过期优先（过期只能转 expired 不得复活，transition 内翻转落盘）+ sweepExpired 批量标记；`POST /api/ai/agent/durable/resume` 追加式新增（requireSessionGuard 链上鉴权 + validateJsonBody 白名单 + resolveReminderPrincipal 派生 Principal，handleDurableError 中文映射 401/403/404/409/410，响应经 publicTaskView 剥离哈希）。提醒 receipt_wait 最小接线两点：①`agentService.js` chat 派生段 +3 行（require+两行调用 registerReminderReceiptWaitBestEffort，仅在 pendingAction.target.type==="reminder" 时登记，waitEvent=action_receipt，context={command,runId,detailId}，expiresAt 与 pendingAction 一致，best-effort 失败仅脱敏日志不影响主链、不回传 token）；②`routes/ai.js` action-receipts 成功段 +13 行（isReminderReceipt 且回执被 M5 全部校验接受后 completeReminderReceiptWait 按 principalKeyHash+command+runId+detailId 精确匹配置 done，找不到/失败均静默不改变 M5 回执语义与响应），M5 四重校验语义零改动。测试 `tools/test-agent-durable-execution.js` 18 组（生命周期全态/终态不可逆/过期不得复活/sweep、token 错误·形态非法·未知 taskId·principal 不符·匿名逐项拒绝、跨进程子进程登记→新 store 实例 resume→子进程读回 resumed、端点 requireSessionGuard 结构断言+无会话 401+合法 resume 200 且响应无 token/哈希、接线绿（chat 自动登记→伪造回执 409 保持 waiting→真实落库+合法回执置 done）/失败回执不置 done/存储故障 chat 主链仍成功（EEXIST 脱敏日志））两轮全绿 18/18，命名命中 regression runner 自动发现正则。验证：`node --check` 五个新/改文件通过；既有回归 test-course-reminder-receipt(21/21)/test-ai-set-current-schedule-receipt(15/15)/test-course-reminder-agent-integration(PASS)/test-agent-run-events(PASS) 全绿；零外部依赖、未改 manifest/kernel-runtime/providers/miniprogram/package.json/runner；grep 盘清 6 个无 FOSU_DATA_DIR 重定向的提醒相关测试均无 agentService.chat 调用（durable 写入不会在既有套件落入仓库），临时目录运行后无残留。【疑似缺陷】无新增；遗留：approval 与通用 waitEvent 目前仅 API+测试覆盖（按 T4 范围不接更多场景），durable 存储尚未纳入定期 sweep 调度（惰性过期已保证正确性，sweep 仅空间回收）。 |

- [2026-07-28T21:24:46+08:00] M6 收尾（主 Agent）：里程碑集成测试实际运行（串行，exit 0）：test:agent-foundation 33/33、test:agent-phase2 11/11、test:agent-phase3 all passed、test:agent-final-convergence all passed、test:ai-competition passed、test:agent-regression 126/126（test-agent-tool-plan-manifest 与 test-agent-durable-execution 均被 runner 自动发现并绿色）。M6 验收口径达成：意图→工具映射单源（toolRegistry 五因子交集 + capabilityRouter manifest 派生兜底，grep 无第三份手工映射）；kernel useToolChain 旧链移除（实证零调用方死代码）单一 PlannerCoordinator 路径、业务恢复逻辑下放 skillRegistry.SKILL_RECOVERY_RULES；MAX_REPLAN 文档口径与代码一致（AGENTS.md:83 与 plannerPolicy 注释对齐 planSchema.MAX_REPLAN=2）；durable 任务跨进程重启可 resume（子进程模拟实证）；敏感信息扫描 M6 全部改动零命中。遗留：durable 存储暂无定期 sweep 调度（惰性过期保证正确性）；approval/通用 waitEvent 仅 API+测试未接场景。建议 commit：refactor(agent): single-source tool planning and lightweight durable execution (M6)。
