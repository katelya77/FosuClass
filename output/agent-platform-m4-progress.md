# Agent 平台化 M4 进度记录

- 2026-07-28T11:50:10+08:00 M4-T1 完成（FollowUpResolver 唯一实现，GoalContract V2 感知）：`server/src/services/ai/planner/followUpResolver.js` 追加式升级——新增主接口 `resolve({ message, goalContractV2, workingState, pendingClarification })` → `{ resolvedIntent, inheritedEntities, followUpMode, provenance, goalId, entities, constraints, confidence }`（非追问透传 = `followUpMode:"none"` + `resolvedIntent:null`，绝不误判）；V2 契约直接消费、V1 载荷经 `goalContractV2.fromV1Contract` 升级（不可升级载荷降级为无契约、绝不抛错，`provenance.contractSource` 标记 v2/v1_upgraded/none），working state 内存储的 `lastGoalContract` 同样经 V2 适配消费。决策顺序：①"不是A，是B" 纠正解析（按 role 替换实体，provenance 标记 correction/replacedRoles/correctedFrom；校区纠正走 constraints 路径）→ ②pendingClarification 槽位补齐（显式参数优先于 state，过期失效，teacher 校验与 legacy 完全一致、class/classroom/course 依 goalResolver 语义泛化）→ ③契约声明式 followUpMode（inherit_last_entity/inherit_active_goal/replace_constraints/fill_pending_clarification/correction，约束合并对齐 goalResolver.mergeConstraints parity，实体继承 lastResolvedEntity→lastEntity→存储契约顺序）→ ④确定性约束门（legacy isConstraintOnlyFollowUp/isCampusSwapOnly + 新增 isDateOnlyFollowUp 日期词门 + 连续节次门，weather/空教室/个人课表/全校索引四族意图重建，未点名约束全继承）→ ⑤"刚才那个班" 回指（inherit_last_entity）。legacy 导出（resolveFollowUp/normalizeConversationWorkingState/applyTurnToWorkingState 等 11 个）零行为变化，toolRegistry(:24,948) 与 goalResolver(:2,12) 调用方不动，M4-T2 再切换。新增 `tools/test-agent-followup-resolver.js`（10 断言组：实体纠正/校区纠正/校区切换/日期切换/回指/透传/V1 升级/pending 补齐/声明式模式/legacy 兼容面，命名命中 run-agent-regression-tests.js 自动发现），fixture 全虚构无网络。验收实际运行：`node --check` 两文件通过；新测试连续两轮 10/10 PASS exit 0；兼容回归 `test-xiaofu-memory-integration`/`test-agent-personal-memory`(+`test-agent-context-memory-flow`)/`test-xiaofu-core-experience`(pass=72)/`test-agent-model-first-understanding`/`test-goal-contract-v2` 全 PASS。【疑似缺陷 ×2，锁定未修】①`parseDateOffset` 分支顺序 /后天/ 先于 /大后天/，"大后天" 永远返回 offset 2（:137-139 不可达分支意图为 3）；②legacy 空教室路径 prev campus 为空时 "campus" 同入 inheritedFields 与 replacedFields（:255-261，weather 分支会过滤 inherited 中的 campus、空教室分支不会，两族口径矛盾）。冻结文件（toolRegistry/goalResolver/goalContractResolver/workingMemory/memoryController/agentService/manifest/package.json/runner）未动，未做任何 git 操作，T2/T3/T4 未进入。
- 2026-07-28T15:05:36+08:00 M4-T2 完成（三处并存 follow-up 解析器退役/委托到唯一实现 followUpResolver.resolve）：①toolRegistry.js 删除 legacy resolveFollowUpIntent 及 4 个私有 helper（weekdayFromFollowUpText/weekFromFollowUpText/periodFromFollowUpText/isFollowUpModifierMessage），resolveIntent 内两处旧调用点合并为单一 resolveUnifiedFollowUpIntent 委托（conversationSlots/lastIntent 仅作缺口折叠，legacy 语义保留）；②understanding/goalResolver.js mergeConstraints(:59-65) 与实体继承段(:132-141) 改为消费 resolve() 的 constraints/entities（V1 契约经 fromV1Contract 升级，原始契约约束最后覆盖以保留 V1 专有键与显式值语义，inheritedEntity 状态读取与 set_current_schedule/意图重建分支零改动）；③runtime/goalContractResolver.js enrichIntentFromWorkingMemory 追问继承（q/type）改消费 normalizeConversationWorkingState（lastResolvedEntity>lastEntity 顺序），周次/星期/时段/校区非追问回填原位保留。计划内行为差异（均无测试锁定旧行为）：个人课表目标+班级工作记忆时"那周三呢"保持个人课表族目标（旧链回落 search_school_index）；enrich 继承优先级由 className>teacherName 收敛为唯一实现的 lastResolvedEntity>lastEntity；声明式 followUpMode 下消息点名的约束切换并入合并基底；toolRegistry 层新增"不是A，是B"纠正与"刚才那个班"回指解析能力（M4-T1 既定语义）。验收实际运行：node --check 三文件通过；test-agent-followup-resolver 连续两轮 10/10 PASS；test-xiaofu-memory-integration/test-agent-personal-memory(+test-agent-context-memory-flow)/test-conversation-memory/test-ai-clarification-followup/test-agent-memory-autonomy(28 cases)/test-agent-e2e-release-scenarios/test-agent-e2e-matrix/test-xiaofu-core-experience(pass=72)/test-agent-model-first-understanding/test-agent-model-first-integration/test-goal-contract-v2 及 resolveIntent 消费方 9 项（goal-intent-regression/open-schedule-goal/public-ai-safety/ai-tool-chain/agent-protocol-v1/teacher-college-keyword/campus-map/course-action-agent/course-reminder-agent-integration）全 PASS；run-agent-regression-tests 全量 123/123 PASS exit 0。grep 证据：resolveFollowUp 与 isConstraintOnlyFollowUp/isCampusSwapOnly/parseContinuousSections/isDateOnlyFollowUp/isAnaphoricEntityReference/parseEntityCorrection 等解析原语仅存于 planner/followUpResolver.js，三处调用点全部委托唯一实现。无【不可调和点】；未改 followUpResolver.js/workingMemory.js/agentService.js/memoryController.js/verification/**/manifest/package.json/runner；git status 仅预期文件（M4-T1 三件 + 本任务三文件 + 本记录）。
- 2026-07-28T15:26:44+08:00 M4-T3 完成（Thread Working State 字段与 GoalContract V2 对齐，含 pendingClarification 统一形态）：八字段盘点结论——workingMemory 已全量存在 activeGoal(:19)/pendingClarification(:56)/lastGoalContract(:41)/lastResolvedEntity(:22)/lastConstraints(:23)/pendingAction(:38)/providerUsed(:39)/understandingSource(:40)，唯一差距是 pendingClarification 原样透传未归一；followUpResolver typed conversation working state 缺 lastGoalContract/providerUsed/understandingSource。对齐动作（全部追加/归一，零破坏）：①memory/workingMemory.js 新增 normalizeStoredPendingClarification(:102-113，统一 5 键形态 {intentName,type,missing,createdAt,expiresAt}，多余键丢弃、时间戳归 Number、畸形降级 null、过期不丢——过期判定仍归消费方)，normalizeWorkingMemory(:217) 接入，normalizeStoredGoalContract V1 升级路径零改动；②planner/followUpResolver.js emptyConversationWorkingState(:50-66) 补齐 lastGoalContract/providerUsed/understandingSource 三字段，新增本地 normalizeStoredPendingClarification(:104-114，与 workingMemory 语义逐行一致，planner 不反向依赖 memory 层)，normalizeConversationWorkingState 接入(:120) 并追加 lastGoalContract 经 coerceGoalContractV2Input 归一(:162，不抛错、垃圾降级 null，与 normalizeStoredGoalContract 同语义)/providerUsed/understandingSource(:163-164)；resolve() 主路径、verificationCoordinator（生产方，:30-36 即规范形态）与 goalContractResolver 零改动——三方消费原本即读同一 5 键形状。conversation 存储 schema 未动（仅内存归一，无数据迁移）。测试：tools/test-agent-followup-resolver.js 追加断言组⑪（既有①-⑩零改动；选此文件而非 test-xiaofu-memory-integration——后者是小程序端字符串匹配测试，不适合承载服务端 working state 形态断言），覆盖：双形态八字段齐备/生产→存储→消费三方 deepStrictEqual 一致/归一只收敛键与类型/过期保留 hydration+消费侧拒绝/updateWorkingMemory set-清除-继承三态/V1 升级路径锁定（legacy_v1_working_memory 标记）/typed state 携带 V2 契约与 provider 字段/legacy fill_pending_entity 零回归。验收实际运行：node --check 三文件通过；test-agent-followup-resolver 连续两轮 11/11 PASS；test-xiaofu-memory-integration/test-agent-personal-memory/test-agent-context-memory-flow/test-conversation-memory 连续两轮全 PASS；相邻回归 test-xiaofu-core-experience(pass=72)/test-ai-clarification-followup/test-agent-memory-autonomy(28 cases)/test-agent-protocol-v2/test-goal-contract-v2 全 PASS。无新增【疑似缺陷】（T1 两处锁定维持原状）。冻结文件未动：memoryController.js/agentService.js/toolResultVerifier.js/departureChainVerifier.js/manifest/package.json/runner；git status 仅预期文件（T1/T2 既有未提交改动原样保留）。

- [2026-07-28T15:33:33+08:00] M4-T4 主 Agent 收尾：里程碑集成测试实际运行（串行）：npm run test:agent-phase2 → 11/11 PASS（会话记忆强制套件）；npm run test:agent-foundation → 33/33 PASS。敏感信息扫描 M4 全部改动零命中。M4 验收口径达成：全仓 grep 仅剩 planner/followUpResolver.js 一个 follow-up 主实现（三处旧解析器 toolRegistry/goalResolver/goalContractResolver 全部委托或退役）；working state 八字段（activeGoal/pendingClarification/lastGoalContract/lastResolvedEntity/lastConstraints/pendingAction/providerUsed/understandingSource）齐备且 pendingClarification 5 键形态三方一致；V1 存储升级路径不变；既有记忆/追问测试零行为回归（T2 期间 regression 123/123）。M4-T1 锁定的 2 处【疑似缺陷】维持锁定未修，留待用户裁定。建议 commit：refactor(agent): converge follow-up resolution on single GoalContract-V2 resolver (M4)。
