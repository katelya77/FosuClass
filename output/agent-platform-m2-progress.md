# Agent 平台化 M2 进度记录

- 2026-07-28T04:02:53+08:00 M2-T1 接手：实现文件 `server/src/services/ai/verification/toolResultVerifier.js`（361 行，未跟踪）已由前一 coder 落盘；复核 `node --check` 通过、require 正常、`verify()` 冒烟结构正确、`verificationPolicy.js` 导出齐全。本任务只补专项测试与进度文件，不重写实现。

- 2026-07-28T04:21:42+08:00 M2-T1 测试收尾：新增 `tools/test-agent-verification.js`（648 行，6 断言组：outputSchema / 四谓词红绿 / emptyResultPolicy 三模式+goalContract 回退 / partialCompletionPolicy / evidencePolicy / 兜底），`node --check` 通过，连续两轮 `node tools/test-agent-verification.js` 均 pass=6 fail=0、exit 0。契约按计划锁定，与实现无签名差异（导出 normalizePolicy/verify）；两处计划口径差异以实现为准锁定：①evidence.fields 是声明的 evidenceFields 原样透传而非"实际取到的字段列表"；②fieldMatchesSlot 与 emptyResultPolicy mode "fail" 在 manifest 中无实例（仅语法层支持），fixture 参照 manifest 形态自构造。未发现疑似实现缺陷；未改动任何实现文件。

- 2026-07-28T04:59:07+08:00 M2-T2 完成（Verification 运行时接线）：`verificationCoordinator.verifyToolResults(execution, intent, goalContract, options)` 新增（policy 工具过滤→`toolResultVerifier.verify`→汇总 `{status, violations, perTool}`→发 `verification.started{toolCount}`/`verification.completed{status, violationCount, tool?}`→结果追加写入 `execution.toolResultVerification` 并折叠进 `execution.verification.errors/ok` 与 `execution.partialCompletion`，供 `deriveExecutionOutcome` 既有 verification/partial 输出消费）；chat() 在 planner 取消检查之后、providerOrchestrator 之前一行接线（`agentService.js` 仅 +15 行注释与调用，19 个导出签名未动）；`toolExecutor.collectToolResults` 纯透传收集 `{toolId, status, result}`；`runEventCatalog` 追加两事件类型+文案+`toolCount/violationCount` 摘要字段；`aguiAdapter` EVENT_MAP 追加 `STEP_STARTED/STEP_FINISHED`；`agentActivityState` 追加 `verifying` 态映射（thinking 门控未动）。分层语义：内容核验只针对工具自报成功（success≠false）的结果；`needContext:true` 为工具层确定性合法空态标记，记 `empty_accepted`；`success:false`/status=failed 的执行失败由 kernel failed-step + resultVerifier 既有链裁决，coordinator 记 `skipped(tool_call_failed)` 不重复惩罚（否则 campus_multi_step_advice 基线 completed→failed 回归）。验收：`node --check` 8 文件全过；`test-agent-verification` 连续两轮 pass=6 fail=0；`test-agent-run-events`（含新事件断言与红/绿/空态/失败工具接线证明）与 `test-agent-activity-state`（含 verifying 映射）连续两轮 PASS；`test:agent-foundation` 32/32、`test:agent-regression` 121/121、`test:ai-competition`、`test:agent-final-convergence`、`test:agent-phase3` 全绿；`git status` 仅 8 个预期修改文件+3 个 M2-T1 未跟踪文件，481 行纯插入 0 删除。【疑似缺陷】`get_course_route` 的 manifest `emptyResultPolicy.codes`（NO_MATCHING_COURSE 等）未覆盖工具真实的无 `code` `needContext` 空态形态（M2-T1 策略缺口，strategy 字段本任务冻结未改，已在 coordinator 以工具自带合法空态标记兜底）。

- 2026-07-28T05:19:26+08:00 M2-T3 完成（出发链实体-时间-地点交叉核验）：新增 `server/src/services/ai/verification/departureChainVerifier.js`（311 行，导出 `verifyDepartureChain`/`isDepartureChainGoal` 及 CHAIN_GOAL_IDS 等常量），覆盖链式目标 `course_action_advice`（"我下一节课在哪，什么时候该出发？"）：课表侧（get_next_course/get_today_courses/get_tomorrow_courses，按路线 date 对齐优先、工具优先级回退、nextCourse→courses[0] 回退，与 getCourseRoute 真实选择一致）与路线侧（get_course_route）结果均可用才核验，否则 skipped 不误报（needContext 合法空态/上游失败/perTool empty_accepted 或 failed 均不重复惩罚）。六规则：CHAIN_NEXT_COURSE_MISSING(硬)/CHAIN_CLASSROOM_MISSING(软)/CHAIN_START_TIME_MISMATCH(硬，任一侧时间不可解析则不评估)/CHAIN_DESTINATION_MISMATCH(硬，地点归一化含楼栋代码与包含 tolerance，参照 campusMapService.extractBuildingCode 惯例)/CHAIN_DEPARTURE_NOT_EARLIER(软，严格早于，相等即违)/通用缓冲出发（preciseRouteAvailable≠true）必进 approximateFlags（field=departureTime，中文"非精确导航"标注，advisory 不影响状态）。`verificationCoordinator.verifyToolResults` 追加式接线：链 violations 以 tool:"departure_chain" 折叠进聚合 violations/violationCount 并按既有规则折叠 failed/partial，summary 追加 `chain`（完整裁定）与 `evidence.approximateFlags` 透出，链 failed 时 kernel errors 追加 {code:TOOL_RESULT_VERIFICATION_FAILED, tool:"departure_chain"}；`verification.completed` 事件载荷形态不变（status/violationCount 反映含链聚合，tool 仍只放真实工具 id）。新增 `tools/test-agent-departure-chain-verification.js`（559 行，9 断言组：门控齐备性/六规则红绿/近似旗标/全绿链与课表侧选择/coordinator 端到端绿红软 skipped）。验收：`node --check` 三文件全过；新测试连续两轮 pass=9 fail=0 exit 0；回归 `test-agent-verification`(pass=6)/`test-agent-run-events`/`test-agent-activity-state` 全 PASS；`git status` 无预期外变动（T2 既有 8 修改+3 未跟踪之外仅新增本任务 2 文件）。冻结文件（manifest/agentService.js/runEventCatalog.js/aguiAdapter.js/toolResultVerifier.js 等）未动，未发现新疑似缺陷。

- 2026-07-28T05:27:28+08:00 M2-T4 完成（主 Agent 收尾）：`tools/test-agent-verification.js` 与 `tools/test-agent-departure-chain-verification.js` 接入 `tools/run-agent-phase3-tests.js`（test-agent-activity-state 之后）；`package.json` 新增 `"test:agent-verification": "node tools/test-agent-verification.js && node tools/test-agent-departure-chain-verification.js"`（主 Agent 独占热点，追加式）。里程碑集成测试实际运行（串行）：`npm run test:agent-phase3` all passed（26 文件，含 2 个新专项）、`npm run test:agent-foundation` 32/32、`npm run test:agent-final-convergence` all passed，exit 均 0。主 Agent 已亲自审查 `runEventCatalog.js` diff（纯追加 11 行：EVENT_TYPES +2、文案 +2 case、publicEventSummary +2 字段，无删除/改名/语义变更）并复跑三个定向测试全绿。M2 全里程碑改动 544 插入 0 删除；敏感信息扫描零命中（fixture 全虚构）；未做任何 git 操作，建议 commit 待用户确认。遗留记录（不阻塞 M2 验收）：`get_course_route` manifest `emptyResultPolicy.codes` 缺口按计划冻结，留 M3 由 manifest 负责方裁定。
