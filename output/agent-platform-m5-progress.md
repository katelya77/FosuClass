# Agent Platform M5 进度

- [2026-07-28T07:52:06+08:00] M5-T1 AG-UI 事件映射 + 工具/卡片标签单源生成器落地：新增 tools/generate-agent-event-map.js（默认写生成物，--check 守卫，确定性无时间戳/随机，CRLF 归一化对齐 capability-compat 先例）。事件映射源=runEventCatalog.js EVENT_TYPES（30 条冻结目录，只读）+aguiAdapter.js EVENT_MAP（30 条现行映射基准，只读），生成器内置覆盖断言（目录有事件无映射/映射有目录外事件即 exit 1，防未来漂移）；生成物落点裁定：server/src/shared/aguiEventMap.generated.js（对齐 school-search 惯例 server/src/shared/*.generated.js，未采用计划文中提及的 services/generated 目录——该目录不存在且 school-search/classroomSearch/teacherSearch 三先例均落 shared）+cloudfunctions/common/aguiEventMap.generated.js（网关侧同源字节级副本；云函数共享模块惯例为 require("../common/...")，getClassScheduleRealtime 等 12+ 函数现状佐证；T2 在 xiaofuAgentGateway 以该相对路径消费）。导出 EVENT_TYPES/EVENT_MAP/mapRunEventType（纯 CommonJS 无依赖，网关侧无云函数特有 API，可直接 require）。标签源=agent-capability-manifest.json（未改动）：TOOL_LABELS 32 键=manifest.tools 全集，值=intents[toolId].displayName（24 条中文），无同名 intent 的 8 工具（get_today_schedule/get_course_route/inspect_schedule_conflicts/navigate_miniprogram_page/create/update/delete/list_course_reminders）回退为 id 本身并在头部注释声明；CARD_TYPE_LABELS 9 键=manifest.cardTypes 全集，manifest 未携带卡片中文名（grep 全清单确认中文 displayName 仅存在于 runtimeModes/intents/actions/miniprogram 四节），值暂为键名占位（头部注释明确非展示文案，T4 消费前需在 manifest 扩展卡片标签权威源或另定回退表）——此为本任务如实记录的 manifest 数据缺口，不在生成器内硬编标签表（否则生成器本身沦为第二份手工源）。落点：server/src/shared/agentLabels.generated.js + miniprogram/shared/agentLabels.generated.js（字节级同源）。【漂移清单】无映射值漂移：网关 EVENT_MAP（index.js:15-25，9 条）逐条与 aguiAdapter 一致，仅为覆盖子集（缺 21 条事件映射）且该常量在网关内无任何引用（mapRunToAgui 由 toolCalls 直接合成事件，死代码状态），T2 切换即消除；标签侧 ai-assistant.js TOOL_LABELS(16 键)/CARD_TYPE_LABELS(20 键) 与 xiaofu-agent-run PUBLIC_TOOL_LABELS(21 键) 含 manifest 不存在的键（如 search_teacher/fosu_rag_retrieve/schedule_result/weather_card），属 T4 收口范围。验收：node tools/generate-agent-event-map.js 生成→--check exit 0；篡改 server 侧生成物→--check exit 1 并点名 stale 文件→恢复 exit 0；5 个新文件 node --check 全过；4 生成物 node require 冒烟通过（含小程序/网关侧，纯数据无 wx/云 API）；生成 EVENT_MAP vs aguiAdapter 现行 EVENT_MAP deepStrictEqual 逐条 diff=0（30/30）、EVENT_TYPES vs catalog diff=0（30/30）；TOOL_LABELS 键 32⊆manifest.tools、CARD_TYPE_LABELS 键 9⊆manifest.cardTypes；双端副本 cmp 字节级一致；重复生成幂等。未触碰 aguiAdapter.js/xiaofuAgentGateway/**/miniprogram 消费方/runEventCatalog.js/manifest/package.json（消费切换=T2/T4，脚本接线留主 Agent/T5）。git status 仅预期 5 个新增（另 docs/xiaofu-agent/agent-platform-migration-plan.md 的 M 为任务开始前已存在的用户改动，未触碰）。
- [2026-07-28T08:12:56+08:00] M5-T2 AG-UI 映射双消费端切换 + xiaofuAgentGateway 鉴权/环境裁决收紧落地。(1) server/src/services/ai/aguiAdapter.js：删除手写 EVENT_MAP 字面量（原 :8-39 共 30 条），改消费生成物 const { EVENT_MAP } = require("../../shared/aguiEventMap.generated")（引用惯例对齐 classroomSearch.js:5 等 server/src/shared/*.generated 无扩展名先例）；导出形态不变（module.exports 仍含 EVENT_MAP+4 函数），冻结语义不变（生成物 Object.freeze）；生成器 generate-agent-event-map.js:26 经 require(adapter) 取 EVENT_MAP 形成自闭环（generator→adapter→generated），渲染字节不变，--check 持续 exit 0 已实测。(2) cloudfunctions/xiaofuAgentGateway/index.js：a) 死代码 EVENT_MAP（原 :15-25，9 条、全文件零引用、mapRunToAgui 由 toolCalls 直接合成事件）纯删除，未消费 ../common/aguiEventMap.generated.js（无引用场景按任务裁定纯删，生成物仍留待 T5 契约测试等消费）；b) 鉴权/环境裁决收紧（原 :201-204）：删除 X-Fosu-Env-Version 默认注入 "trial"（body.envVersion || "trial"）与 X-Fosu-Session 空串注入，改为仅透传客户端显式上报值——envVersion 取 body.envVersion 或入站 x-fosu-env-version 头、session 取 body.sessionToken 或入站 x-fosu-session 头，均 trim 后非空才设置对应上游头，否则省略；对齐主入口语义依据：server/src/routes/ai.js resolveRequestRuntimeDecision(:95-115) 从 query/header/body.context 取客户端上报 envVersion、session 仅由 req.fosuSession（verified token）派生，runtimeModeService.resolveRuntimeMode 对空 envVersion fail-closed 为 public（reason env_version_not_develop_or_trial）、对无授权 session 的 trial 上报默认旗标下裁决 public（competition_not_authorized），网关透明透传后上游裁决与客户端直连主入口完全等价，兼容层功能（离线健康分支/agui 映射/agent.v2 转发/session 透传/memoryMode 默认 local_only）全部保留未删。行为对照：无 session 且无上报→收紧前注入 trial 提权，收紧后省略头、上游 fail-closed public；无 session 自报 trial→收紧前后均转发 trial 但由上游裁决（默认旗标→public），网关自身不再新增任何提权；有 session+上报→前后均透传。(3) 测试适配：无需适配——全仓 grep xiaofuAgentGateway / require(cloudfunctions/xiaofuAgentGateway) 仅命中文档、cloudbaserc.json 与函数自身，tools/test-coze-gateway-live.js 实测覆盖 cozeToolGatewayService（Coze 工具网关，另一组件）、tools/test-cloudbase-ai-router.js 覆盖小程序端 router（wx mock），均无对该网关的旧断言，故无"无 session 注入 trial"锁定断言需按流程改写；两测试原样运行全绿。(4) 验收实测：node --check 两改动文件通过；node tools/generate-agent-event-map.js --check exit 0；adapter.EVENT_MAP vs 手写原表 30/30 deepStrictEqual 且 vs 生成物 deepStrictEqual、Object.isFrozen=true、键序一致；node tools/test-agui-adapter.js 14/14、test-xiaofu-runs-transport.js / test-xiaofu-runtime-ui.js / test-agent-activity-state.js / test-agent-run-events.js / test-cloudbase-ai-router.js / test-coze-gateway-live.js(12/12) 全 exit 0；收紧证明=本地回环捕获上游请求头 5 场景（A 无 session 无上报→两头部省略不注入 trial；B 无 session 自报 trial→透传 trial 且无 session 头；C body.sessionToken+envVersion→均透传；D 入站 HTTP 头 x-fosu-session/x-fosu-env-version→均透传；E 离线健康分支与 mapRunToAgui/parseHttpBody 导出保留、死代码 EVENT_MAP 不存在）全 PASS，另以 runtimeModeService 实证上游 fail-closed（空 envVersion→public/env_version_not_develop_or_trial；无授权 session+trial→public/competition_not_authorized；授权 session+trial→trial/server_session_authorized）。【疑似缺陷】无新增；T1 已记录标签侧 manifest 数据缺口留 T4。git status 仅预期：本两改动文件 + 本进度文件（M5-T1 未提交产物原样保留，无任何 git 操作）。
- [2026-07-28T17:21:00+08:00] M5-T3 小程序客户端去伪造 + 提醒语义收回服务端落地（分支 refactor/xiaofu-agent-platform-v2，仅 services 层 + 既有测试适配，未进 T4/T5）。(1) miniprogram/services/aiAssistantService.js 离线应答去伪造：standardizeClientFallback 删除伪造 runId（原 createClientRunId("offline")→runId:""）、删除 normalizeClientFallbackSteps（原 :1484-1504 伪造 status:"done" 步骤列表）改 steps:[]/plan:[]、status 统一 degraded（原 plain 为 completed）、新增 normalizeOfflineEvidence 强制离线证据 verified:false/complete:false（保留 term/week/checkedAt/sources 事实字段）；buildCachedPersonalScheduleResponse 证据 verified:true,complete:true（原 :1672-1680）改 false——不再绕过页面 inferEvidenceLabel 降级守卫（fallback 且 !complete→"本地能力结果"），help/navigation 两离线构造器同类 verified:true 同步改 false；卡片徽标"离线降级/本机缓存"如实披露链保留。(2) reportPipelineStatus（原 :958-970）及 16 处调用点全删，本地流水线伪造文案（"正在查询课表/已生成卡片/已核验课表数据"）退役，替换为 reportLocalDegradedStatus 单一如实披露"正在处理本机结果"（offlineChat 入口一次）。(3) 提醒拦截退役：删除 parseLeadMinutesFromMessage/isReminderAssistMessage/buildLocalReminderAssistResponse（原 :1731-1798 正则解析分钟数+saveUserPreferences 本地改写+伪造 update_user_preference toolCalls）及 offlineChat 拦截分支与两个导出；提醒类消息全走服务端链（manage_course_reminders 工具+confirmReminder/confirmationProof 链不变），离线按普通降级应答不落地任何偏好/提醒写入。(4) miniprogram/services/aiTransportRouter.js 合成文案清查结论=零改动："正在理解你的问题"类文案已不存在（全仓 grep 无命中），现存 request.submitted"正在建立校园任务"（test-xiaofu-runs-transport.js:82-87 锁定中性约束：禁含理解/查询/核验）、run.accepted/run.status_unavailable 均为真实传输事实；runs 轮询与 AI_AGENT_RUNS_TRANSPORT_ENABLED 回滚开关、敏感信息守卫、语音链路未动。(5) 测试适配 2 处（旧→新→理由）：test-xiaofu-online-server-first-v2.js evidence.complete===true→status degraded+无 runId+steps[]+verified/complete===false+"离线降级"徽标断言（离线不得声称核验），另新增问候 degraded/runId 断言与"默认提前45分钟提醒我"离线不改写偏好/无伪造 toolCalls 回归块；test-campus-assistant-copy-audit.js requiredPhrases"正在匹配查询内容/正在查询校园信息"→"正在处理本机结果"（伪造流水线文案随删除退役，副本审计改验如实披露文案，未弱化任何如实标注断言）。(6)【阻塞·报主 Agent】提醒 ActionReceipt 闭环无法在本任务边界内收口：a) server/src/routes/ai.js:357 action-receipts 端点硬编码仅接受 command==="setCurrentSchedule"，其余一律 400 COMMAND_UNKNOWN——服务端缺提醒类 receipt 契约，按任务指令未擅改 server；b) receipt 执行绑定与徽标改写位于 packageXiaofu/pages/ai-assistant/ai-assistant.js:4009-4017/4122-4147/4238-4245（页面层，T3 禁改）。提醒写入本身已是服务端确认链路（POST /api/ai/agent/reminders/configure 或 confirmationProof 通道，幂等+按 principal 落库），缺失的仅是记忆提交回执与本机偏好可见性回传，建议 T4/T5 或专项扩展 receipts 端点命令集+页面绑定后收口。(7) 验收实测：node --check 三改动文件通过；验收清单 test-xiaofu-runs-transport/runtime-ui/agent-activity-state/agent-ui-v2/memory-integration/course-reminder-client/course-reminder-ui 及 grep 命中 30+ 既有测试全绿；npm run test:agent-foundation 33/33、test:agent-regression 123/123、test:ai-competition、test:agent-final-convergence、test:agent-phase2 11/11、test:agent-phase3 全过；git status --porcelain 仅 aiAssistantService.js+两个测试文件为本任务新增改动（其余为 T1/T2 既有未提交项）。
- [2026-07-28T18:14:38+08:00] M5-T3b 提醒 ActionReceipt 闭环收口（T3 上报阻塞解除，仅本任务范围，未进 T4/T5）。(1) server/src/routes/ai.js action-receipts 端点追加式扩展：白名单=setCurrentSchedule+REMINDER_RECEIPT_COMMANDS（createCourseReminder/deleteReminder，经 actionCommandContract.getActionDefinition+receiptRequired 只读过滤，契约缺失即自动出列）；未在契约内 command 仍 400 COMMAND_UNKNOWN；提醒类目标校验以服务端提醒存储达成态为准（create 要求按 idempotencyKey 真实落库→否则 409 REMINDER_TARGET_NOT_FOUND，delete 要求目标已不存在→否则 409 REMINDER_TARGET_STILL_EXISTS，principal 由 resolveReminderPrincipal 会话派生），课表索引校验原样保留。(2) memoryController.commitActionReceipt 追加式：RECEIPT_COMMANDS 白名单扩展后 pendingAction 四重校验（command/runId/expiresAt/target+cloud_sync 授权）对提醒命令同样生效，校验链其他逻辑零改动；提醒命令 mutation 分支不改写 currentScheduleTarget，仅清 pendingAction+记 lastResolvedEntity(type=reminder)，persist intentName=manage_course_reminders。(3) agentService.js 新增 deriveReminderPendingAction（chat 时从 create/delete_course_reminder 工具结果确定性派生 pendingAction，与 attachReminderConfirmation 同一 reminderIdempotencyKey 纯函数同输入→pending.target.detailId 恒等于卡片 idempotencyKey；setCurrentSchedule 派生优先，提醒为回退），作为 module.exports 追加导出。(4) courseReminderService 新增只读 findByIdempotencyKey（经 operations 幂等映射反查，空 key 返回 null 不抛错）。(5) 页面 ai-assistant.js：postAgentActionReceipt 改为返回 Promise(ack|null)（setCurrentSchedule 链忽略返回值行为不变）；performReminderConfirmation 成功写入后回传 {command:createCourseReminder,runId:activeRunId,appliedTarget:{type:reminder,detailId:卡片 idempotencyKey,name}}，仅 ack.success===true 才 markReminderCardCreated+胶囊"完成 · 提醒已创建"；回执拒绝/网络异常走新 markReminderCardReceiptUnconfirmed（徽标"已创建未回执"、胶囊"（回执未确认）"），不伪造已回执。(6) 新增 tools/test-course-reminder-receipt.js 21 项全绿：端点 7 项（新命令接受/未知 400/目标达成态正反对/target 缺失/failed 不提交）+四重校验 9 项（逐项拒绝+create/delete 接受+不改写课表目标+local_only 不落盘）+全链 1 项（chat 派生 pendingAction=detailId 与卡片 idempotencyKey 一致且 runId 绑定→服务写入→端点接受→cloud_sync 提交）+客户端 3 项（载荷契约/失败不标已创建/异常降级）；既有 test-ai-set-current-schedule-receipt(15)/test-course-reminder-agent-integration/client/ui/api/credits/delivery-fallback/reminders/wechat-subscription/full-width-entry/test-xiaofu-runtime-ui/test-publisher-receipts 全绿；npm run test:agent-foundation&&test:agent-regression&&test:ai-competition&&test:agent-final-convergence&&test:agent-phase2&&test:agent-phase3 链式 exit 0。【已知边界】客户端仅 confirmReminder 卡（create）接线回执；deleteReminder 端点已接受但客户端删除路径在提醒面板组件（本任务范围外），其 pendingAction 派生已就绪待后续里程碑接线；回执 runId 取 activeRunId，旧卡跨 run 点击将被服务端 runId 校验如实拒绝并降级提示（符合防伪造语义）。

- [2026-07-28T19:00:55+08:00] M5-T4 工具/卡片中文标签单源化落地（分支 refactor/xiaofu-agent-platform-v2，双手工表删除+消费切生成物，未进 T5）。(1) 权威源层级执行：裁定 2 生成器扩展取数——TOOL_LABELS 优先级改为 manifest.tools[id].displayName→同名 intent displayName→id 占位，CARD_TYPE_LABELS 改读新增 manifest.cardTypeLabels（actions 节只有 UI 动作 displayName、miniprogram.capabilities 是快捷入口注册表，均无 8 个无名工具与 9 种卡片的对应中文名，第 2 步无直接来源）→触发裁定 3 manifest 追加式扩展：tools 17 键补 displayName（9 个与页面手表现行值漂移的键取现行值保 UI 零变化：get_campus_weather=天气/get_course_weather_advice=天气建议/search_campus_place/get_campus_route/get_classroom_location=校园地图/diagnose_data_status=数据状态/explain_personal_import=导入指引/recommend_meeting_time=时间推荐/clarify_missing_slot=追问；8 个原 id 占位工具按语义定名：get_today_schedule=今日课表/get_course_route=课程路线/inspect_schedule_conflicts=课表冲突检查/navigate_miniprogram_page=页面跳转/create/update/delete_course_reminder=创建/更新/删除课程提醒/list_course_reminders=课程提醒列表）+ 新增顶层 cardTypeLabels 平行映射 9 键（值取页面手表现行值：空教室/课表/教师/课程/校区天气/数据状态/指引/提醒/结果；cardTypes 字符串数组结构未动——capabilityManifestService :164 依赖 .includes 字符串语义，平行映射是追加式等价物）；未改任何 tool id/intent/skill/allowedTools/verification 结构。(2) 双手工表删除+消费切换：页面 ai-assistant.js 删 TOOL_LABELS(16 键)/CARD_TYPE_LABELS(20 键)，改 require("../../../shared/agentLabels.generated.js")（对齐 :48 aiCapabilityRegistry.generated 既有先例），mapToolName 的 search_school_index 特判"正在查询全校课程"保留（旧表"全校索引"条目本被特判遮蔽=死条目）；CARD_TITLE_FALLBACKS 是卡片标题回退表非标签表，不在本任务范围保留；新增 CARD_TYPE_LABEL_ALIASES 三条（weather_card→weather、schedule_result→schedule、schedule_status→diagnosis，归并真实键且标签值不变）。组件 xiaofu-agent-run/index.js 删 PUBLIC_TOOL_LABELS(20 键)，publicToolLabel 保留标识改读生成物 TOOL_LABELS+原正则/"校园工具"回退（两调用点实证不可达：页面供给的 steps 恒带中文 label 或"步骤 N"，WXML 唯一消费方 ai-assistant.wxml:177 绑 displaySteps），test-xiaofu-agent-ui-v2.js:85 存在性断言零适配通过。(3) 非 manifest 键逐键处置（裁定 4）：工具 3 键 search_school_schedule_local/fosu_rag_retrieve/safety_guard=客户端内部伪工具名，标签展示死路径（normalizeToolCall 全仓零调用、toolCalls 无 WXML 渲染、T3 后离线 steps 仅 safety_guard 一条且其 tool 字段不渲染、sourceCategory 新旧均归"校园服务"）→删除；卡片 11 键中 3 键别名归并（见上），余 8 键（clarification/schedule_candidate/personal_schedule/school_knowledge/navigation/help/import_guide/not_found）因 typeLabel 全仓 write-only 无任何渲染消费回退"结果"，schedule_candidate 另无生产者=死键。(4) 验收实测：node tools/generate-agent-event-map.js 生成→--check exit 0；生成器/双生成物/页面/组件 5 文件 node --check 全过；双生成物 cmp 字节一致+require 冒烟（32 工具/9 卡片全中文）；等价 node -e 逐键比对：TOOL 13/16 相等+0 漂移+3 死路径键列出，CARD 12/20 相等（9 manifest+3 别名归并）+8 死路径键列出；capabilityManifestService 加载冒烟 OK（cardTypeLabels 9 键可读）+test-agent-capability-manifest+test-agent-capabilities-endpoint 全绿；npm run test:xiaofu-agent-ui（3 件）+test-xiaofu-runtime-ui+test-ai-built-in-example-matrix/test-ai-action-contract/test-ai-card-polish-contract/test-ai-today-card-no-inactive-render/test-campus-assistant-copy-audit/test-course-reminder-ui/test-xiaofu-quick-actions-ui/test-xiaofu-runs-transport/test-xiaofu-online-server-first-v2/test-xiaofu-core-experience(72) 全绿；AGENTS.md 必跑 test:agent-foundation/test:agent-regression/test:ai-competition/test:agent-final-convergence 四大套件全绿（exit 0，无基线失败）。grep 证据：两文件手工表字面量已消失，消费点引用 ../../../shared/agentLabels.generated.js（页面 :57/:538/:553、组件 :3/:7）；git status 仅 T1-T3b 既有+T4 预期文件（manifest/生成器/双生成物/页面/组件/本进度文件）。【疑似缺陷·锁定未改】a) server/src/services/ai/responseComposer/index.js TOOL_PUBLIC_LABELS(19 键"正在..."进行中文案，含非 manifest 键 open_schedule/campus_multi_step_advice)=第三份手工表，语义为进度文案非标签且不在 T4 双表交付边界，留待后续里程碑收口；b) 页面 mapToolName 内嵌 search_school_index 进度式特判与标签职责混杂（为守 UI 零变化保留）。测试适配：零（无任何旧断言锁手工表位置需改写；test-xiaofu-agent-ui-v2.js:85 经 publicToolLabel 标识保留原样通过）。

- [2026-07-28T19:07:50+08:00] M5-T5 主 Agent 收尾：新增 tools/test-agent-event-map-contract.js（7 组：--check 守卫/双副本字节一致/EVENT_MAP↔catalog 完整性与 mapRunEventType 全域/aguiAdapter 消费生成物无手工表/网关无第二 EVENT_MAP 且无默认 trial 注入/标签全量 manifest 来源无占位/页面与组件消费生成物双表消失），连续运行 exit 0。主 Agent 接线：package.json 新增 generate:/check:agent-event-map 脚本；check:agent-event-map 接入 test:agent-unified-chain 守卫位；契约测试接入 run-agent-phase3-tests.js。里程碑集成测试实际运行（串行，exit 0）：npm run test:agent-phase3 all passed（27 文件，含新契约测试）、npm run test:xiaofu-agent-ui 三件全过、npm run test:agent-foundation 33/33、npm run test:agent-unified-chain 全链路通过（含三契约 check 守卫 + convergence 52/0）。M5 验收口径达成：两份 EVENT_MAP 手工同步消失（check 守卫锁定）；离线应答 grep 无伪造 runId/verified:true/update_user_preference（0 命中）；提醒全流程服务端确认 + ActionReceipt 回传（createCourseReminder/deleteReminder 白名单 + 四重校验 + 回传成功才标"已创建"，test-course-reminder-receipt 21 项）；标签单一来源（manifest 追加 17 displayName + cardTypeLabels 9 键，双份手工表删除）；runs 传输回滚开关 AI_AGENT_RUNS_TRANSPORT_ENABLED 未动可用。敏感信息扫描 M5 全部改动零命中。遗留：responseComposer TOOL_PUBLIC_LABELS 第三份手工表（进度文案非标签，留 M6/M7 收口）；提醒面板组件 xiaofu-reminder-sheet 删除提醒未接 receipt（端点与派生已就绪，接线留后续）；回执 runId 跨 run 旧卡边界（如实降级，符合防伪造语义）。建议 commit：feat(xiaofu): single-source AG-UI event map and purge client-side synthesized states (M5)。
