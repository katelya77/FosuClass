const { createSkillCatalog } = require("./src/skillCatalog");
const { createSkillPublicationAdapter } = require("./src/skillPublicationAdapter");

module.exports = Object.freeze({
  createSkillCatalog,
  createSkillPublicationAdapter,
});
