<view class="memory-sheet" wx:if="{{visible}}">
  <view class="sheet-handle"></view>
  <view class="memory-head">
    <view>
      <view class="memory-title">记忆设置</view>
      <view class="memory-subtitle">控制小佛如何保存任务上下文</view>
    </view>
    <button class="sheet-close" bindtap="onClose">关闭</button>
  </view>

  <view class="memory-mode-list">
    <button
      class="memory-mode-item {{mode === 'local_only' ? 'active' : ''}}"
      data-mode="local_only"
      bindtap="onSelectMode"
    >
      <view class="memory-mode-title">仅保存在本机</view>
      <view class="memory-mode-desc">完整对话只在手机本地。服务端不保存会话内容。</view>
    </button>
    <button
      class="memory-mode-item {{mode === 'session_state' ? 'active' : ''}}"
      data-mode="session_state"
      bindtap="onSelectMode"
    >
      <view class="memory-mode-title">保存会话状态</view>
      <view class="memory-mode-desc">登录后可保存结构化任务槽位与摘要，便于连续追问。不保存完整消息。</view>
    </button>
    <button
      class="memory-mode-item {{mode === 'cloud_sync' ? 'active' : ''}}"
      data-mode="cloud_sync"
      bindtap="onSelectMode"
    >
      <view class="memory-mode-title">同步脱敏对话</view>
      <view class="memory-mode-desc">需主动开启。仅保存脱敏后的有限最近消息，可跨设备恢复，可随时关闭和清除。</view>
    </button>
  </view>

  <view class="memory-note">
    <view>会保存：任务类型、班级/教师/教室等槽位、简短摘要、数据来源引用。</view>
    <view>不会保存：学号密码 Cookie Token、完整个人课表、原始 XLS、模型隐藏推理。</view>
    <view>保留周期：会话状态约 7 天；同步对话约 30 天。关闭同步后可选择是否删除云端数据。</view>
  </view>

  <view class="memory-actions">
    <button class="memory-action" bindtap="onClearCurrent">清除当前对话记忆</button>
    <button class="memory-action danger" bindtap="onClearAll">清除全部云端记忆</button>
  </view>
</view>
