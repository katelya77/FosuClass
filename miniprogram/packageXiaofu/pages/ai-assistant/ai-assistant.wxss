@import "../../styles/xiaofu-tokens.wxss";

.ai-page {
  box-sizing: border-box;
  display: flex;
  height: 100vh;
  min-height: 100vh;
  flex-direction: column;
  overflow: hidden;
  padding: calc(8rpx + env(safe-area-inset-top)) 16rpx 200rpx;
  padding-bottom: 200rpx;
  background: #f6f4f1;
}

button {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  border: 0;
  background: transparent;
  text-align: inherit;
}

button::after {
  border: 0;
}

.card {
  box-sizing: border-box;
  border-radius: 16rpx;
  background: #ffffff;
}

.xiaofu-header {
  box-sizing: border-box;
  position: relative;
  z-index: 30;
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  gap: 12rpx;
  min-height: 96rpx;
  max-height: 112rpx;
  padding: 10rpx 4rpx 12rpx;
  border: 0;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
}

.xiaofu-header-compact {
  min-height: 96rpx;
  padding: 8rpx 2rpx 10rpx;
  gap: 10rpx;
  box-shadow: none;
  border-bottom: 1rpx solid rgba(42, 38, 34, 0.06);
}

.xiaofu-header-compact .xiaofu-avatar-wrap {
  flex-basis: 48rpx;
  width: 48rpx;
  height: 48rpx;
  border-radius: 14rpx;
  box-shadow: none;
  background: #ebe7e1;
}

.xiaofu-header-compact .xiaofu-avatar,
.xiaofu-header-compact .xiaofu-avatar-text {
  width: 40rpx;
  height: 40rpx;
  line-height: 40rpx;
  font-size: 20rpx;
  border-radius: 12rpx;
}

.agent-status-capsule {
  box-sizing: border-box;
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  align-self: stretch;
  width: 100%;
  min-height: 88rpx;
  margin: 4rpx 0 2rpx;
  padding: 18rpx 22rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.08);
  border-radius: 44rpx;
  background: #2a2622;
  color: #ffffff;
  box-shadow: 0 8rpx 22rpx rgba(42, 38, 34, 0.14);
  transition: min-height 180ms ease, border-radius 180ms ease, background-color 180ms ease;
}

.agent-status-capsule.expanded {
  min-height: 116rpx;
  border-radius: 28rpx;
}

.agent-status-capsule.waiting_confirmation {
  background: #6c3d2e;
}

.agent-status-capsule.complete {
  background: #2f6f4e;
}

.agent-status-capsule.network_error {
  background: #7d302e;
}

.in-app-reminder-banner {
  box-sizing: border-box;
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  width: 100%;
  min-height: 128rpx;
  margin: 14rpx 0 2rpx;
  padding: 18rpx 18rpx 18rpx 20rpx;
  border: 1rpx solid rgba(47, 111, 78, 0.2);
  border-left: 6rpx solid #2f6f4e;
  border-radius: 18rpx;
  background: #f3f7f4;
}

.in-app-reminder-banner.schedule_change {
  border-color: rgba(198, 40, 40, 0.2);
  border-left-color: #c62828;
  background: #fff6f4;
}

.in-app-reminder-copy {
  flex: 1 1 auto;
  min-width: 0;
}

.in-app-reminder-eyebrow {
  color: #2f6f4e;
  font-size: 19rpx;
  font-weight: 650;
  line-height: 1.3;
}

.in-app-reminder-banner.schedule_change .in-app-reminder-eyebrow {
  color: #a82121;
}

.in-app-reminder-title {
  overflow: hidden;
  margin-top: 5rpx;
  color: #2a2622;
  font-size: 26rpx;
  font-weight: 650;
  line-height: 1.35;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.in-app-reminder-detail {
  overflow: hidden;
  margin-top: 5rpx;
  color: #766f67;
  font-size: 20rpx;
  line-height: 1.4;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.in-app-reminder-actions {
  flex: 0 0 auto;
  margin-left: 16rpx;
  text-align: right;
}

.in-app-reminder-link {
  color: #2f6f4e;
  font-size: 19rpx;
  font-weight: 650;
  line-height: 1.4;
}

.in-app-reminder-dismiss {
  min-width: 104rpx;
  min-height: 64rpx;
  margin: 8rpx 0 0;
  padding: 0 14rpx;
  border: 0;
  border-radius: 12rpx;
  background: #ffffff;
  color: #5f5851;
  font-size: 20rpx;
  line-height: 64rpx;
}

.in-app-reminder-dismiss::after {
  border: 1rpx solid rgba(42, 38, 34, 0.1);
  border-radius: 12rpx;
}

.agent-status-dot {
  flex: 0 0 14rpx;
  width: 14rpx;
  height: 14rpx;
  margin-right: 16rpx;
  border: 3rpx solid rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  background: #d8d1c9;
}

.agent-status-capsule.querying .agent-status-dot,
.agent-status-capsule.composing .agent-status-dot,
.agent-status-capsule.thinking .agent-status-dot,
.agent-status-capsule.understanding .agent-status-dot {
  background: #f3b34c;
}

.agent-status-capsule.complete .agent-status-dot {
  background: #d7f0df;
}

.agent-status-copy {
  flex: 1 1 auto;
  min-width: 0;
}

.agent-status-text {
  overflow: hidden;
  font-size: 23rpx;
  font-weight: 650;
  line-height: 1.35;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.agent-status-detail {
  margin-top: 7rpx;
  color: rgba(255, 255, 255, 0.72);
  font-size: 19rpx;
  line-height: 1.45;
}

.agent-status-chevron {
  flex: 0 0 40rpx;
  width: 40rpx;
  margin-left: 12rpx;
  color: rgba(255, 255, 255, 0.64);
  font-size: 28rpx;
  text-align: center;
}

.xiaofu-conversation-primary {
  margin-top: 0;
  font-size: 30rpx;
  font-weight: 650;
  color: #2a2622;
  line-height: 1.3;
}

.xiaofu-status-line {
  margin-top: 4rpx;
  max-width: 100%;
  overflow: hidden;
  color: #9a9188;
  font-size: 20rpx;
  font-weight: 500;
  line-height: 1.3;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Keep product name in DOM for accessibility/tests without repeating large header title */
.xiaofu-title-sr {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.ai-mode-row-slim {
  margin-top: 8rpx;
  margin-bottom: 4rpx;
}

.run-compact {
  margin-top: 8rpx;
  padding: 6rpx 12rpx;
  border-radius: 999rpx;
  background: #f3f0eb;
  color: #6b635a;
  font-size: 20rpx;
  line-height: 1.3;
  align-self: flex-start;
}

.evidence-collapsed {
  opacity: 0.85;
}

.evidence-collapse {
  margin-top: 6rpx;
  color: #8a8178;
  font-size: 20rpx;
}

/* Default feedback-row removed from product UI; feedback only via long-press */

.header-menu-list {
  display: flex;
  flex-direction: column;
  width: 100%;
  padding: 0 8rpx 24rpx;
  box-sizing: border-box;
}

.header-menu-row {
  display: flex;
  align-items: center;
  width: 100%;
  min-height: 88rpx;
  padding: 18rpx 16rpx;
  margin: 0;
  border-radius: 0;
  border-bottom: 1rpx solid rgba(40, 36, 32, 0.06);
  background: transparent;
  text-align: left;
}

.header-menu-row-title {
  flex: 1 1 auto;
  color: #2b2622;
  font-size: 28rpx;
  font-weight: 600;
}

.header-menu-row-meta {
  flex: 0 1 auto;
  margin-right: 8rpx;
  color: #8a8178;
  font-size: 22rpx;
  line-height: 1.35;
  white-space: normal;
  word-break: break-word;
}

.header-menu-row-arrow {
  flex: 0 0 auto;
  color: #b0a8a0;
  font-size: 32rpx;
  line-height: 1;
}

.header-menu-row.danger .header-menu-row-title {
  color: #c62828;
}

.header-menu-sheet-full .task-panel-head {
  width: 100%;
}

.xiaofu-avatar-wrap {
  flex: 0 0 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 56rpx;
  height: 56rpx;
  overflow: hidden;
  border-radius: 16rpx;
  background: #ffffff;
  box-shadow: 0 8rpx 18rpx rgba(198, 40, 40, 0.12);
}

.xiaofu-avatar {
  width: 48rpx;
  height: 48rpx;
  border-radius: 14rpx;
}

.xiaofu-avatar-text {
  width: 48rpx;
  height: 48rpx;
  border-radius: 14rpx;
  background: #c62828;
  color: #ffffff;
  font-size: 24rpx;
  font-weight: 900;
  line-height: 48rpx;
  text-align: center;
}

.xiaofu-title-block {
  flex: 1 1 auto;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: center;
  min-width: 0;
  overflow: hidden;
}



.xiaofu-conversation-sub {
  max-width: 100%;
  margin-top: 6rpx;
  overflow: hidden;
  color: #4a433c;
  font-size: 22rpx;
  font-weight: 500;
  line-height: 1.3;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.xiaofu-chip-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-top: 8rpx;
  max-width: 100%;
  min-width: 0;
}

.xiaofu-chip {
  max-width: 100%;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
  background: #f3f0eb;
  color: #6b635a;
  font-size: 18rpx;
  line-height: 1.3;
  white-space: nowrap;
}

.xiaofu-chip.soft {
  background: #efece7;
  color: #8a8178;
}

.xiaofu-chip.status-online {
  color: #2f855a;
  background: rgba(47, 133, 90, 0.1);
}

.xiaofu-chip.status-offline,
.xiaofu-chip.status-warn {
  color: #c05621;
  background: rgba(192, 86, 33, 0.1);
}

.send-btn.cancel {
  background: #4a433c !important;
  color: #fff !important;
  min-width: 72rpx;
}

.task-card-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14rpx;
  margin-top: 18rpx;
}

.task-entry-card {
  margin: 0;
  padding: 20rpx 18rpx;
  border-radius: 18rpx;
  background: #ffffff;
  border: 1rpx solid rgba(40, 36, 32, 0.06);
  box-shadow: 0 8rpx 20rpx rgba(40, 36, 32, 0.04);
  text-align: left;
  line-height: 1.35;
}

.task-entry-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #2d2925;
}

.task-entry-desc {
  margin-top: 8rpx;
  font-size: 22rpx;
  color: #8a8178;
}

.fallback-banner {
  margin: 8rpx 0 4rpx;
  padding: 12rpx 14rpx;
  border-radius: 14rpx;
  background: #fff7ed;
  color: #9a3412;
  font-size: 22rpx;
  line-height: 1.4;
  border: 1rpx solid rgba(194, 120, 40, 0.18);
}



.send-btn.icon-send {
  min-width: 72rpx;
  width: 72rpx;
  padding: 0;
  font-size: 28rpx;
  font-weight: 700;
}


.xiaofu-title-line {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.xiaofu-title {
  flex: 0 0 auto;
  color: #1f2937;
  font-size: 30rpx;
  font-weight: 900;
  line-height: 1;
}

.xiaofu-actions {
  flex: 0 0 120rpx;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 6rpx;
  width: 110rpx;
  min-width: 110rpx;
}

.xiaofu-icon-btn {
  flex: 0 0 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 56rpx;
  height: 56rpx;
  min-width: 56rpx;
  max-width: 56rpx;
  min-height: 56rpx;
  padding: 0;
  border-radius: 14rpx;
  background: transparent;
  color: #4a433c;
  font-size: 30rpx;
  font-weight: 600;
  line-height: 56rpx;
  text-align: center;
}

.ai-mode-row {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  justify-content: center;
  min-height: 48rpx;
  margin-top: 12rpx;
  padding: 0 8rpx;
}

.quick-action-scroll {
  flex: 0 0 auto;
  width: 100%;
  min-width: 0;
  height: 60rpx;
  margin-top: 8rpx;
  overflow: hidden;
  white-space: nowrap;
}

.quick-action-list {
  display: inline-flex;
  gap: 8rpx;
  padding: 1rpx 10rpx 5rpx 2rpx;
}

.quick-action-pill {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  width: 156rpx;
  height: 52rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.82);
  color: #374151;
  font-size: 21rpx;
  font-weight: 900;
  line-height: 52rpx;
  box-shadow: 0 6rpx 18rpx rgba(31, 41, 55, 0.05);
}

.quick-action-pill.today {
  color: #1f5f8b;
}

.quick-action-pill.room {
  color: #2f6f4e;
}

.quick-action-pill.classroom {
  color: #7a4f28;
}

.quick-action-pill.teacher {
  color: #8a4b4b;
}

.quick-action-pill.xls,
.quick-action-pill.sync {
  color: #6b4d16;
}

.quick-action-icon {
  flex: 0 0 auto;
  width: 31rpx;
  height: 31rpx;
}

.quick-action-label {
  max-width: 94rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.quick-nav-segment {
  box-sizing: border-box;
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 4rpx;
  width: 186rpx;
  max-width: 60vw;
  height: 42rpx;
  padding: 3rpx;
  overflow: hidden;
  border: 1rpx solid rgba(214, 222, 232, 0.92);
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.68);
  box-shadow: 0 4rpx 12rpx rgba(31, 41, 55, 0.035);
}

.quick-nav-option {
  box-sizing: border-box;
  flex: 1 1 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: auto;
  min-width: 0;
  height: 34rpx;
  border-radius: 999rpx;
  background: transparent;
  color: #5f6b7c;
  font-size: 20rpx;
  font-weight: 900;
  line-height: 34rpx;
  text-align: center;
  box-shadow: none;
}

.quick-nav-option.active {
  background: #f2faf5;
  color: #2f6f4e;
  box-shadow: 0 3rpx 10rpx rgba(47, 111, 78, 0.08);
}

.quick-nav-option text {
  display: block;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.history-trim-tip {
  flex: 0 0 auto;
  margin: 6rpx 4rpx 0;
  color: #8a6f64;
  font-size: 19rpx;
  font-weight: 700;
  line-height: 1.4;
  text-align: center;
}

.message-scroll {
  box-sizing: border-box;
  width: 100%;
  flex: 1;
  min-height: 0;
  margin-top: 10rpx;
  padding: 4rpx 0 28rpx;
}

.message-bottom-anchor {
  width: 1rpx;
  height: 20rpx;
}

.empty-welcome {
  box-sizing: border-box;
  width: 100%;
  max-height: none;
  margin: 48rpx 0 24rpx;
  padding: 12rpx 0 8rpx;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
}

.empty-brand {
  color: #2a2622;
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1.25;
  letter-spacing: 0.02em;
}

.empty-title {
  margin-top: 12rpx;
  color: #6b635a;
  font-size: 28rpx;
  font-weight: 500;
  line-height: 1.5;
}

.empty-desc {
  margin-top: 10rpx;
  color: #667085;
  font-size: 23rpx;
  line-height: 1.45;
}

button.proactive-insight {
  box-sizing: border-box;
  display: block;
  width: 100%;
  max-width: none;
  min-height: 210rpx;
  margin: 30rpx 0 0;
  padding: 26rpx 28rpx 24rpx;
  border: 1rpx solid #e5dfd7;
  border-left: 8rpx solid #c62828;
  border-radius: 18rpx;
  background: #ffffff;
  box-shadow: 0 8rpx 24rpx rgba(42, 38, 34, 0.06);
  text-align: left;
}

.proactive-insight.schedule_change {
  border-left-color: #9a5b2e;
}

.proactive-insight.free_day,
.proactive-insight.day_finished {
  border-left-color: #2f6f4e;
}

.proactive-eyebrow {
  color: #8a8178;
  font-size: 20rpx;
  font-weight: 650;
  letter-spacing: 0.08em;
  line-height: 1.3;
}

.proactive-title {
  margin-top: 12rpx;
  color: #2a2622;
  font-size: 31rpx;
  font-weight: 700;
  line-height: 1.38;
}

.proactive-detail {
  margin-top: 12rpx;
  color: #6b635a;
  font-size: 23rpx;
  line-height: 1.58;
}

.proactive-link {
  margin-top: 18rpx;
  color: #a51f1f;
  font-size: 23rpx;
  font-weight: 650;
  line-height: 1.4;
}

.contextual-action-row {
  display: flex;
  gap: 10rpx;
  margin-top: 16rpx;
}

.contextual-action {
  flex: 1 1 0;
  min-width: 0;
  min-height: 88rpx;
  padding: 20rpx 10rpx;
  border-bottom: 2rpx solid #d8d1c9;
  color: #4f4841;
  font-size: 22rpx;
  font-weight: 600;
  line-height: 1.35;
  text-align: center;
}

.empty-safe-note {
  margin-top: 18rpx;
  color: #b0a8a0;
  font-size: 18rpx;
  font-weight: 500;
  line-height: 1.3;
}

.message-row {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
  margin-bottom: 28rpx;
}

.message-row.user {
  justify-content: flex-end;
}

.assistant-avatar,
.assistant-avatar-spacer {
  flex: 0 0 auto;
  width: 40rpx;
  height: 40rpx;
}

.assistant-avatar {
  border-radius: 12rpx;
  background: #ebe7e1;
  color: #c0392b;
  font-size: 18rpx;
  font-weight: 700;
  line-height: 40rpx;
  text-align: center;
  box-shadow: none;
}

.assistant-avatar-spacer {
  opacity: 0;
}

.message-row.no-avatar .assistant-avatar-spacer {
  width: 40rpx;
}

.avatar-logo {
  width: 32rpx;
  height: 32rpx;
  margin: 4rpx;
  border-radius: 10rpx;
}

.message-body {
  min-width: 0;
}

.message-row.assistant .message-body {
  width: calc(100% - 52rpx);
  max-width: calc(100% - 52rpx);
}

.message-row.user .message-body {
  display: flex;
  max-width: 78%;
  flex-direction: column;
  align-items: flex-end;
}

/* Flat assistant content — no heavy white bubble card */
.message-content-wrap.assistant-plain {
  box-sizing: border-box;
  max-width: 100%;
  padding: 4rpx 4rpx 2rpx;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
  border: 0;
}

.message-content-wrap.user-bubble {
  box-sizing: border-box;
  max-width: 100%;
  padding: 16rpx 20rpx;
  border-radius: 20rpx 20rpx 6rpx 20rpx;
  background: #c0392b;
  box-shadow: none;
  position: relative;
}

.message-bubble {
  box-sizing: border-box;
  max-width: 100%;
  padding: 16rpx 18rpx;
  border-radius: 18rpx;
  background: transparent;
  box-shadow: none;
}

.message-row.assistant .message-bubble {
  border: 0;
  background: transparent;
  box-shadow: none;
}

.message-row.user .message-bubble {
  border-radius: 20rpx 20rpx 6rpx 20rpx;
  background: #c0392b;
  box-shadow: none;
}

.message-content {
  color: #2a2622;
  font-size: 28rpx;
  line-height: 1.62;
  white-space: pre-wrap;
  word-break: break-word;
}

.message-content-text {
  display: inline;
  white-space: pre-wrap;
  word-break: break-word;
}

.message-row.assistant .message-content-text {
  user-select: text;
}

.message-row.user .message-content {
  color: #ffffff;
  font-size: 28rpx;
  line-height: 1.55;
}

.message-meta {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-top: 8rpx;
  color: #b0a8a0;
  font-size: 18rpx;
  line-height: 1.3;
  opacity: 0.85;
}

.message-meta-text {
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.message-row.user .message-meta {
  color: rgba(255, 255, 255, 0.65);
  justify-content: flex-end;
}

.send-fail-retry {
  margin-top: 8rpx;
  color: #c0392b;
  font-size: 22rpx;
  font-weight: 600;
}

.tool-strip {
  display: flex;
  flex-wrap: nowrap;
  gap: 7rpx;
  margin: 8rpx 2rpx 0;
  overflow: hidden;
}

.tool-chip {
  max-width: 100%;
  height: 30rpx;
  padding: 0 10rpx;
  border-radius: 999rpx;
  overflow: hidden;
  background: #eef7ff;
  color: #25658f;
  font-size: 17rpx;
  font-weight: 800;
  line-height: 30rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.tool-chip.success {
  background: #edf8f1;
  color: #2f6f4e;
}

.tool-chip.failed {
  background: #fff1f1;
  color: #c62828;
}

.tool-chip.skipped {
  background: #f2f4f7;
  color: #667085;
}

.tool-chip.running {
  background: #fff7e8;
  color: #9a5b08;
}

.evidence-strip {
  margin: 8rpx 2rpx 0;
  max-width: 92%;
  overflow: hidden;
  color: #6b7280;
  font-size: 21rpx;
  line-height: 1.45;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.ai-card-list {
  width: 100%;
  margin-top: 10rpx;
}

.ai-result-card {
  margin-top: 10rpx;
  padding: 18rpx;
  border: 1rpx solid rgba(231, 237, 245, 0.96);
  box-shadow: 0 12rpx 32rpx rgba(31, 41, 55, 0.08);
}

.ai-result-card.card-error {
  border-color: #fde2e2;
  background: #fffafa;
}

.card-header-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12rpx;
  min-width: 0;
}

.card-title-block {
  min-width: 0;
  flex: 1;
}

.card-title {
  display: -webkit-box;
  overflow: hidden;
  color: #1f2937;
  font-size: 26rpx;
  font-weight: 900;
  line-height: 1.32;
  text-overflow: ellipsis;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.card-subtitle {
  display: -webkit-box;
  overflow: hidden;
  margin-top: 6rpx;
  color: #6b7280;
  font-size: 21rpx;
  line-height: 1.42;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.card-type {
  flex: 0 0 auto;
  max-width: 112rpx;
  padding: 6rpx 10rpx;
  border-radius: 999rpx;
  overflow: hidden;
  background: #f4f6f8;
  color: #6b7280;
  font-size: 17rpx;
  font-weight: 900;
  line-height: 1.2;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.badge-row {
  display: flex;
  flex-wrap: wrap;
  gap: 7rpx;
  margin-top: 12rpx;
}

.badge {
  max-width: 190rpx;
  min-height: 30rpx;
  padding: 3rpx 10rpx;
  border-radius: 999rpx;
  overflow: hidden;
  background: #f2f7ff;
  color: #356b93;
  font-size: 17rpx;
  font-weight: 800;
  line-height: 1.25;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-items {
  margin-top: 12rpx;
  border-top: 1rpx solid #edf0f5;
}

.card-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14rpx;
  min-height: 62rpx;
  padding: 13rpx 0;
  border-bottom: 1rpx solid #f2f4f7;
}

.card-item:last-child {
  border-bottom: 0;
}

.item-main {
  min-width: 0;
  flex: 1;
}

.item-title {
  overflow: hidden;
  color: #1f2937;
  font-size: 24rpx;
  font-weight: 900;
  line-height: 1.25;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-type-empty-room .item-title {
  font-size: 26rpx;
}

.item-subtitle {
  display: -webkit-box;
  overflow: hidden;
  margin-top: 5rpx;
  color: #7a8494;
  font-size: 20rpx;
  line-height: 1.35;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.item-value {
  flex: 0 0 auto;
  max-width: 172rpx;
  overflow: hidden;
  color: #4b5563;
  font-size: 21rpx;
  font-weight: 900;
  line-height: 1.25;
  text-align: right;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-type-empty-room .item-value {
  color: #c62828;
}

.card-overflow {
  width: 100%;
  height: 46rpx;
  margin-top: 8rpx;
  border-radius: 12rpx;
  background: #f4f6f8;
  color: #5f6b7c;
  font-size: 20rpx;
  font-weight: 900;
  line-height: 46rpx;
  text-align: center;
}

.card-overflow.expanded {
  background: #eef2f6;
  color: #374151;
}

.card-actions {
  display: flex;
  gap: 10rpx;
  margin-top: 16rpx;
}

.card-actions.one-action .card-action-primary {
  width: 100%;
}

.card-actions.two-actions .card-action-primary {
  flex: 1;
}

.card-action-primary {
  min-width: 0;
  height: 56rpx;
  padding: 0 14rpx;
  border-radius: 14rpx;
  overflow: hidden;
  background: #c62828;
  color: #ffffff;
  font-size: 21rpx;
  font-weight: 900;
  line-height: 56rpx;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-type-school-knowledge .card-action-primary,
.card-type-navigation .card-action-primary,
.card-type-schedule-candidate .card-action-primary,
.card-type-schedule-status .card-action-primary,
.card-type-guide .card-action-primary {
  background: #eef6f1;
  color: #2f6f4e;
}

.secondary-action-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-top: 10rpx;
}

.card-action-secondary {
  width: auto;
  max-width: 240rpx;
  height: 46rpx;
  padding: 0 14rpx;
  border-radius: 999rpx;
  overflow: hidden;
  background: #f1f3f6;
  color: #4b5563;
  font-size: 20rpx;
  font-weight: 800;
  line-height: 46rpx;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.filtered-hint {
  margin-top: 10rpx;
  color: #98a2b3;
  font-size: 18rpx;
  font-weight: 700;
  line-height: 1.35;
}

.card-disclaimer {
  margin-top: 12rpx;
  padding-top: 10rpx;
  border-top: 1rpx dashed #e5eaf1;
  color: #98a2b3;
  font-size: 17rpx;
  line-height: 1.35;
}

.weather-card {
  position: relative;
  overflow: hidden;
  padding: 18rpx;
  border-radius: 16rpx;
  background: linear-gradient(135deg, #fffafa 0%, #ffffff 54%, #eef8f2 100%);
  border: 1rpx solid rgba(231, 237, 245, 0.96);
}

.weather-card-head,
.weather-main,
.weather-metrics,
.weather-timeline {
  position: relative;
  z-index: 1;
}

.weather-card-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12rpx;
}

.weather-campus {
  color: #1f2937;
  font-size: 27rpx;
  font-weight: 900;
  line-height: 1.25;
}

.weather-status,
.weather-updated {
  margin-top: 5rpx;
  color: #667085;
  font-size: 20rpx;
  font-weight: 800;
  line-height: 1.35;
}

.weather-updated {
  max-width: 248rpx;
  margin-top: 0;
  text-align: right;
}

.weather-main {
  display: flex;
  align-items: center;
  gap: 18rpx;
  margin-top: 18rpx;
}

.weather-temp {
  color: #172033;
  font-size: 72rpx;
  font-weight: 900;
  line-height: 1;
}

.weather-temp.missing {
  max-width: 210rpx;
  font-size: 30rpx;
  line-height: 1.25;
}

.weather-temp text {
  font-size: 30rpx;
}

.weather-facts {
  flex: 1;
  min-width: 0;
  color: #536174;
  font-size: 22rpx;
  font-weight: 800;
  line-height: 1.6;
}

.weather-icon {
  position: relative;
  flex: 0 0 auto;
  width: 88rpx;
  height: 74rpx;
}













.card-weather-rain 

.weather-metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 8rpx;
  margin-top: 18rpx;
}

.weather-metric {
  min-width: 0;
  padding: 12rpx 8rpx;
  border-radius: 12rpx;
  background: rgba(255, 255, 255, 0.7);
  text-align: center;
}

.weather-metric text {
  color: #7a8494;
  font-size: 18rpx;
  font-weight: 800;
}

.weather-metric view {
  margin-top: 6rpx;
  color: #172033;
  font-size: 20rpx;
  font-weight: 900;
  line-height: 1.2;
}

.weather-timeline {
  display: flex;
  gap: 8rpx;
  margin-top: 14rpx;
  overflow: hidden;
}

.weather-hour {
  flex: 1;
  min-width: 0;
  padding: 10rpx 6rpx;
  border-radius: 10rpx;
  background: rgba(248, 250, 252, 0.9);
  color: #536174;
  font-size: 17rpx;
  font-weight: 800;
  line-height: 1.35;
  text-align: center;
}

.weather-hour text {
  display: block;
  margin-top: 4rpx;
}

.weather-advice {
  position: relative;
  z-index: 1;
  margin-top: 14rpx;
  padding-top: 12rpx;
  border-top: 1rpx dashed rgba(203, 213, 225, 0.9);
  color: #4b5563;
  font-size: 22rpx;
  font-weight: 800;
  line-height: 1.45;
}

.weather-source {
  position: relative;
  z-index: 1;
  margin-top: 8rpx;
  color: #8a94a6;
  font-size: 18rpx;
  font-weight: 700;
  line-height: 1.35;
}

@keyframes weatherBreath {
  0%, 100% { opacity: 0.45; transform: scale(0.92); }
  50% { opacity: 0.8; transform: scale(1.08); }
}

@keyframes weatherCloud {
  0%, 100% { transform: translateX(-4rpx); }
  50% { transform: translateX(5rpx); }
}

@keyframes weatherDrop {
  0% { opacity: 0; transform: translateY(-8rpx); }
  45% { opacity: 0.8; }
  100% { opacity: 0; transform: translateY(14rpx); }
}

.suggestion-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 14rpx;
}

.suggestion-chip {
  width: auto;
  max-width: 100%;
  min-height: 52rpx;
  padding: 0 16rpx;
  border-radius: 12rpx;
  overflow: hidden;
  background: transparent;
  color: #8b3a32;
  font-size: 24rpx;
  font-weight: 550;
  line-height: 52rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
  box-shadow: none;
  border: 1rpx solid rgba(192, 57, 43, 0.18);
}

.slow-tip {
  margin: 10rpx 0 18rpx 56rpx;
  padding: 14rpx 16rpx;
  border-radius: 14rpx;
  background: #fff7ed;
  color: #9a5b08;
  font-size: 21rpx;
  line-height: 1.45;
}

/* Explainable task trajectory — collapsed by default; no technical dump */
.task-trajectory {
  margin: 8rpx 0 12rpx;
  padding: 16rpx 18rpx;
  border-radius: 16rpx;
  background: #f8fafc;
  border: 1rpx solid #e8eef5;
}

.trajectory-section {
  margin-bottom: 12rpx;
}

.trajectory-section:last-of-type {
  margin-bottom: 0;
}

.trajectory-label {
  font-size: 20rpx;
  font-weight: 700;
  color: #667085;
  margin-bottom: 4rpx;
}

.trajectory-body,
.trajectory-plan-item,
.trajectory-exec-item {
  font-size: 22rpx;
  line-height: 1.5;
  color: #344054;
}

.trajectory-exec-item {
  display: flex;
  gap: 8rpx;
  align-items: flex-start;
}

.trajectory-mark {
  color: #059669;
  font-weight: 700;
  flex-shrink: 0;
}

.trajectory-collapse {
  margin-top: 10rpx;
  font-size: 20rpx;
  color: #98a2b3;
  text-align: right;
}

.run-compact {
  margin: 6rpx 0 8rpx;
  padding: 8rpx 0;
  font-size: 22rpx;
  color: #667085;
}

.sending-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin: 12rpx 0 18rpx 56rpx;
  color: #667085;
  font-size: 22rpx;
  font-weight: 700;
}

.sending-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: #c62828;
  animation: aiPulse 1s ease-in-out infinite;
}

@keyframes aiPulse {
  0%, 100% {
    opacity: 0.35;
    transform: scale(0.82);
  }
  50% {
    opacity: 1;
    transform: scale(1);
  }
}

.sheet-mask {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 38;
  background: rgba(31, 41, 55, 0.22);
}

.bottom-sheet {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 40;
  box-sizing: border-box;
  max-height: 72vh;
  padding: 12rpx 18rpx calc(18rpx + env(safe-area-inset-bottom));
  border-radius: 24rpx 24rpx 0 0;
  background: #ffffff;
  box-shadow: 0 -18rpx 48rpx rgba(31, 41, 55, 0.16);
}

.sheet-handle {
  width: 58rpx;
  height: 6rpx;
  margin: 0 auto 14rpx;
  border-radius: 999rpx;
  background: #d9e0ea;
}

.task-panel-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 14rpx;
  min-width: 0;
  margin-bottom: 12rpx;
}

.panel-title-wrap {
  flex: 1;
  min-width: 0;
}

.task-panel-title {
  color: #1f2937;
  font-size: 28rpx;
  font-weight: 900;
  line-height: 1.25;
}

.task-panel-subtitle {
  overflow: hidden;
  margin-top: 5rpx;
  color: #7a8494;
  font-size: 20rpx;
  line-height: 1.35;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.sheet-close {
  flex: 0 0 auto;
  width: 78rpx;
  height: 44rpx;
  border-radius: 999rpx;
  background: #f3f5f7;
  color: #596579;
  font-size: 20rpx;
  font-weight: 800;
  line-height: 44rpx;
  text-align: center;
}

.task-section-scroll {
  box-sizing: border-box;
  max-height: calc(72vh - 124rpx - env(safe-area-inset-bottom));
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
}

.task-section {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10rpx;
  margin-bottom: 18rpx;
}

.task-section:last-child {
  margin-bottom: 0;
  padding-bottom: calc(18rpx + env(safe-area-inset-bottom));
}

.task-section-title {
  grid-column: 1 / -1;
  margin: 6rpx 2rpx 0;
  color: #8a4b4b;
  font-size: 20rpx;
  font-weight: 900;
  line-height: 1.25;
}

.task-item {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
  width: 100%;
  min-height: 108rpx;
  margin-top: 0;
  padding: 14rpx 12rpx;
  border-radius: 16rpx;
  background: #f8fafc;
  text-align: left;
}

.task-icon {
  flex: 0 0 auto;
  width: 44rpx;
  height: 44rpx;
}

.task-copy {
  flex: 1;
  min-width: 0;
}

.task-label,
.task-desc {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.task-label {
  color: #1f2937;
  font-size: 23rpx;
  font-weight: 900;
  line-height: 1.25;
}

.task-desc {
  margin-top: 5rpx;
  color: #6b7280;
  font-size: 19rpx;
  font-weight: 700;
  line-height: 1.25;
}

.task-section-loading {
  height: 220rpx;
  color: #8a94a6;
  font-size: 22rpx;
  font-weight: 800;
  line-height: 220rpx;
  text-align: center;
}

.header-menu-sheet {
  max-height: 36vh;
}

.capability-sheet {
  max-height: 76vh;
}

.capability-scroll {
  box-sizing: border-box;
  max-height: calc(76vh - 124rpx - env(safe-area-inset-bottom));
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
}

.capability-section {
  margin-bottom: 20rpx;
}

.capability-title {
  margin: 0 2rpx 10rpx;
  color: #8a4b4b;
  font-size: 21rpx;
  font-weight: 900;
  line-height: 1.25;
}

.capability-list {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
}

.capability-chip {
  max-width: 100%;
  min-height: 54rpx;
  padding: 0 18rpx;
  border-radius: 12rpx;
  background: #f8fafc;
  color: #374151;
  font-size: 23rpx;
  font-weight: 800;
  line-height: 54rpx;
  text-align: left;
}

.capability-map-btn {
  width: 100%;
  height: 68rpx;
  margin-top: 4rpx;
  border-radius: 14rpx;
  background: #c62828;
  color: #ffffff;
  font-size: 25rpx;
  font-weight: 900;
  line-height: 68rpx;
  text-align: center;
}

.privacy-sheet {
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
}

.privacy-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  min-width: 0;
}

.privacy-copy {
  flex: 1;
  min-width: 0;
}

.privacy-title {
  color: #1f2937;
  font-size: 25rpx;
  font-weight: 900;
  line-height: 1.25;
}

.privacy-desc {
  display: -webkit-box;
  overflow: hidden;
  margin-top: 7rpx;
  color: #667085;
  font-size: 21rpx;
  line-height: 1.45;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 3;
}

.privacy-switch {
  flex: 0 0 auto;
  transform: scale(0.82);
  transform-origin: right center;
}

.privacy-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14rpx;
  margin-top: 16rpx;
  color: #667085;
  font-size: 20rpx;
  font-weight: 800;
}

.privacy-footer text {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.privacy-btn {
  flex: 0 0 auto;
  height: 46rpx;
  min-width: 98rpx;
  padding: 0 14rpx;
  border-radius: 999rpx;
  background: #c62828;
  color: #ffffff;
  font-size: 20rpx;
  font-weight: 900;
  line-height: 46rpx;
  text-align: center;
}

.privacy-btn.secondary {
  background: #f1f3f6;
  color: #4b5563;
}

.composer {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 30;
  box-sizing: border-box;
  padding: 10rpx 16rpx calc(10rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid rgba(42, 38, 34, 0.06);
  background: rgba(246, 244, 241, 0.96);
  box-shadow: none;
}

.composer-inner {
  display: flex;
  align-items: flex-end;
  gap: 10rpx;
}

.composer-plus-btn {
  flex: 0 0 64rpx;
  width: 64rpx;
  height: 64rpx;
  border-radius: 16rpx;
  background: #ebe7e1;
  color: #4a433c;
  font-size: 32rpx;
  font-weight: 500;
  line-height: 64rpx;
  text-align: center;
}

.voice-btn {
  flex: 0 0 64rpx;
  width: 64rpx;
  height: 64rpx;
  border-radius: 16rpx;
  background: #ffffff;
  border: 1rpx solid rgba(42, 38, 34, 0.1);
  color: #4a433c;
  font-size: 22rpx;
  font-weight: 650;
  line-height: 64rpx;
  text-align: center;
}

.voice-btn.recording {
  background: #fef2f2;
  border-color: #ef4444;
  color: #b91c1c;
}

.voice-btn.disabled {
  background: #eef2f7;
  color: #94a3b8;
}

.composer-input-wrap {
  position: relative;
  box-sizing: border-box;
  flex: 1;
  min-width: 0;
  min-height: 64rpx;
  max-height: 220rpx;
  border: 1rpx solid rgba(42, 38, 34, 0.1);
  border-radius: 18rpx;
  background: #ffffff;
}

.composer-input {
  box-sizing: border-box;
  width: 100%;
  min-height: 64rpx;
  max-height: 220rpx;
  padding: 16rpx 18rpx;
  border: 0;
  border-radius: 18rpx;
  background: transparent;
  color: #2a2622;
  font-size: 28rpx;
  line-height: 1.45;
}

.composer-custom-placeholder {
  position: absolute;
  left: 18rpx;
  right: 18rpx;
  top: 50%;
  z-index: 1;
  overflow: hidden;
  color: #a5adba;
  font-size: 24rpx;
  line-height: 1.2;
  pointer-events: none;
  text-overflow: ellipsis;
  transform: translateY(-50%);
  white-space: nowrap;
}

.voice-status {
  height: 30rpx;
  margin-top: 6rpx;
  color: #64748b;
  font-size: 18rpx;
  font-weight: 700;
  line-height: 30rpx;
  text-align: center;
}

.send-btn {
  flex: 0 0 auto;
  width: 72rpx;
  min-width: 72rpx;
  height: 64rpx;
  border-radius: 16rpx;
  background: #c0392b;
  color: #ffffff;
  font-size: 28rpx;
  font-weight: 700;
  line-height: 64rpx;
  text-align: center;
}

.send-btn.disabled {
  background: #d4cec6;
  color: #ffffff;
  opacity: 0.72;
}

.send-btn.icon-send {
  min-width: 72rpx;
  width: 72rpx;
  padding: 0;
}

/* Settings sheet system */
.settings-sheet-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8rpx 8rpx 16rpx 20rpx;
}

.settings-sheet-title {
  color: #2a2622;
  font-size: 32rpx;
  font-weight: 700;
}

.sheet-close-icon {
  width: 64rpx;
  height: 64rpx;
  min-width: 64rpx;
  border-radius: 32rpx;
  background: #f3f0eb;
  color: #6b635a;
  font-size: 28rpx;
  line-height: 64rpx;
  text-align: center;
}

.settings-sheet-scroll {
  max-height: 62vh;
  padding-bottom: calc(16rpx + env(safe-area-inset-bottom));
}

.settings-section {
  margin-bottom: 12rpx;
  padding: 0 8rpx;
}

.settings-section-label {
  padding: 12rpx 16rpx 8rpx;
  color: #9a9188;
  font-size: 22rpx;
  font-weight: 600;
}

.settings-row {
  display: flex;
  align-items: center;
  width: 100%;
  min-height: 96rpx;
  padding: 16rpx 12rpx;
  gap: 14rpx;
  border-bottom: 1rpx solid rgba(42, 38, 34, 0.06);
}

.settings-row-icon {
  flex: 0 0 48rpx;
  width: 48rpx;
  height: 48rpx;
  border-radius: 12rpx;
  background: #f3f0eb;
  color: #6b635a;
  font-size: 22rpx;
  line-height: 48rpx;
  text-align: center;
}

.settings-row-body {
  flex: 1 1 auto;
  min-width: 0;
}

.settings-row .header-menu-row-title {
  flex: none;
  width: 100%;
  color: #2a2622;
  font-size: 28rpx;
  font-weight: 600;
  white-space: normal;
  overflow: visible;
  text-overflow: clip;
}

.settings-row-desc {
  margin-top: 4rpx;
  color: #9a9188;
  font-size: 22rpx;
  line-height: 1.4;
  white-space: normal;
  word-break: break-word;
}

.settings-row-switch {
  padding-right: 8rpx;
}

.settings-switch {
  flex: 0 0 auto;
  transform: scale(0.9);
}

.settings-section-danger .header-menu-row-title {
  color: #b91c1c;
}

.plus-action-list {
  padding: 8rpx 20rpx calc(24rpx + env(safe-area-inset-bottom));
}

.plus-action-row {
  display: block;
  width: 100%;
  min-height: 88rpx;
  padding: 22rpx 8rpx;
  border-bottom: 1rpx solid rgba(42, 38, 34, 0.06);
  color: #2a2622;
  font-size: 28rpx;
  font-weight: 550;
  text-align: left;
  line-height: 1.4;
}

.xiaofu-settings-sheet {
  max-height: 78vh;
}

.composer-plus-sheet {
  max-height: 56vh;
}

@media (max-width: 360px) {
  .ai-page {
    padding-left: 12rpx;
    padding-right: 12rpx;
  }

  .quick-action-pill {
    width: 154rpx;
  }

  .xiaofu-header {
    gap: 8rpx;
    padding-left: 10rpx;
    padding-right: 10rpx;
  }

  .xiaofu-avatar-wrap {
    width: 52rpx;
    height: 52rpx;
  }

  
.xiaofu-title-line {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.xiaofu-title {
    font-size: 28rpx;
  }

  .xiaofu-actions {
    flex-basis: 104rpx;
    width: 104rpx;
    min-width: 104rpx;
    gap: 4rpx;
  }

  .xiaofu-icon-btn {
    flex-basis: 50rpx;
    width: 50rpx;
    height: 50rpx;
    min-width: 50rpx;
    max-width: 50rpx;
    min-height: 50rpx;
    line-height: 50rpx;
  }

  .quick-action-label {
    max-width: 86rpx;
  }

  .message-row.user .message-body {
    max-width: 84%;
  }

  .in-app-reminder-banner {
    align-items: flex-start;
  }

  .in-app-reminder-actions {
    margin-left: 8rpx;
  }

  .in-app-reminder-link {
    display: none;
  }

  .in-app-reminder-dismiss {
    min-width: 88rpx;
    margin-top: 0;
  }

  .send-btn {
    width: 104rpx;
  }
}

.weather-icon-css{width:72rpx;height:72rpx;border-radius:50%;background:#f5b942;opacity:.9;}

@media (prefers-color-scheme: dark) {
  .ai-page {
    background: #181614;
  }

  .xiaofu-header-compact {
    border-bottom-color: rgba(255, 255, 255, 0.08);
  }

  .xiaofu-conversation-primary,
  .empty-brand,
  .proactive-title,
  .contextual-action {
    color: #f4f0ea;
  }

  .empty-title,
  .proactive-detail {
    color: #bcb3aa;
  }

  .proactive-insight {
    border-color: #3d3833;
    border-left-color: #e05b55;
    background: #24211e;
    box-shadow: none;
  }

  .proactive-insight.schedule_change {
    border-left-color: #c58350;
  }

  .proactive-insight.free_day,
  .proactive-insight.day_finished {
    border-left-color: #68a982;
  }

  .proactive-link {
    color: #ef7771;
  }

  .in-app-reminder-banner {
    border-color: rgba(104, 169, 130, 0.3);
    border-left-color: #68a982;
    background: #202620;
  }

  .in-app-reminder-banner.schedule_change {
    border-color: rgba(224, 91, 85, 0.28);
    border-left-color: #e05b55;
    background: #2a1e1d;
  }

  .in-app-reminder-eyebrow,
  .in-app-reminder-link {
    color: #86c49e;
  }

  .in-app-reminder-banner.schedule_change .in-app-reminder-eyebrow {
    color: #ef7771;
  }

  .in-app-reminder-title {
    color: #f4f0ea;
  }

  .in-app-reminder-detail {
    color: #bcb3aa;
  }

  .in-app-reminder-dismiss {
    background: #312c27;
    color: #e5ded6;
  }

  .contextual-action {
    border-bottom-color: #4a433d;
  }
}

@media (prefers-reduced-motion: reduce) {
  .agent-status-capsule,
  .composer,
  .send-btn,
  .voice-btn {
    transition: none !important;
    animation: none !important;
  }
}
